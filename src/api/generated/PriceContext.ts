// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { PriceContextHistoryPoint } from "./PriceContextHistoryPoint";
import type { PriceContextRatios } from "./PriceContextRatios";

export type PriceContext = { lastClose: number, lastDate: string, 
/**
 * Percentage points already (e.g. `-1.2` renders as "-1.2 %"), matching
 * the shared `formatFinancialValue` percentage contract — never a 0..1
 * fraction.
 */
changeAbs: number, changePct: number, currency: string, week52High: number, week52Low: number, week52HighDistPct: number, week52LowDistPct: number, marketCap: number | null, ratios: PriceContextRatios, history: Array<PriceContextHistoryPoint>, fetchedAt: string, 
/**
 * Present only when there is nothing to render; every other field then
 * carries an inert default (`0.0` / `""` / `[]`) and must be ignored —
 * the section renders its empty state instead of the readout.
 */
emptyReason?: "no_quotes" | "unmapped_ticker", };
