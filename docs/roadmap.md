# Roadmap

This roadmap is **forward-looking**: it covers active and upcoming work plus unscheduled future study. **Under continuous release ([ADR 0090](adr/0090-github-canonical-forge-and-continuous-release.md)) it is organized as functional epics in dependency order, not version milestones** — a version number is set by a PR's `release:*` label on merge day, so future work carries no `vX.Y.Z`. Delivered history keeps its version numbers here and in [CHANGELOG.md](../CHANGELOG.md) (authoritative per-version release notes) + [Kanban Archive](kanban-archive.md) (completed-card detail); live epic/task status lives in GitHub Issues + the "Brawler board" (see [tracking](kanban.md)).

Doc map: [CLAUDE.md](../CLAUDE.md) § Required Reading. Related references: [Engineering Workflow](engineering-workflow.md), [GitHub Issues & Board Tracking](kanban.md), [Product Spec](product-spec.md), and [Source Strategy](source-strategy.md).

## Roadmap Principles

- Build from local-first foundations toward source ingestion and AI.
- Keep every epic/PR demoable — under continuous release every merge is a shippable app.
- Work must close on real working behavior against the real local runtime, real source, real API, or real agent named by the epic.
- Use test samples, mocks, and seed data only as intermediate development steps and in deterministic automated tests; do not treat them as completion evidence unless a milestone explicitly says it is a mock/sample-only spike.
- Do not introduce cloud services in v1.
- Keep contracts and docs updated with each change (in the PR that changes behavior).
- Make local build/test commands the primary interface; CI runs the same `make` targets ([ADR 0090](adr/0090-github-canonical-forge-and-continuous-release.md)).
- Use Nix from the first scaffold for reproducible WSL2 Ubuntu 24.04 development.
- Keep CI feedback fast and secret-free by default; conservative (standard runners, no macOS, no scheduled jobs).
- Prefer lean, behavior-focused tests over broad brittle suites.
- Keep secrets in the OS keychain and non-secret settings in SQLite.
- Use SemVer-style `0.x.y` versions from the first scaffold.
- The local entitlement module supports optional and future gated entitlements, but normal open-core desktop use does not require a license key.

## Delivered

Milestones through `v0.38.0` are shipped. This roadmap does not restate completed work: the authoritative, per-version release history is [CHANGELOG.md](../CHANGELOG.md), and completed-card detail (through ~`v0.24.x`) is in [Kanban Archive](kanban-archive.md). The recent delivered arc:

- Through `v0.31.0` — local-first foundations, GPW ESPI/EBI and Polish media/research sources, the company workspace, notebooks and claims, YouTube transcription, the general AI analysis framework, developer-mode/diagnostics, watchlist management, import/export, portable Windows packaging, modularization, and the research workspace (questions, evidence links, briefs, reminders, digest).
- `v0.35.0` — Multi-provider AI (Claude + OpenAI alongside Gemini, all free with a user-supplied key).
- `v0.34.0`, `v0.36.0`–`v0.37.0` — Company Fundamentals: financial facts data model, AI KPI extraction with mandatory confirmation, and the fundamentals panel with KPI charts. Scope and the KPI taxonomy are fixed in [ADR 0027](adr/0027-company-fundamentals-scope.md).
- `v0.38.0` — Search and data safety hardening: unified FTS5 global search across all stored content (companies, watchlists, feed items, notes, transcript segments, events, briefs, digests), automatic rotating local database backups with restore, pre-migration snapshots, and a WAL connection pool for concurrent background jobs. Boundaries in [ADR 0032](adr/0032-search-and-backup-boundaries.md); feed-retention policy designed in [ADR 0033](adr/0033-feed-retention-policy.md) for a later milestone.
- `v0.39.0` — Interpretative AI layer (static foundation): the two-layer AI architecture and the on-device interpretative layer as capability contracts (`Classifier`, `SimilarityProvider`, `Matcher`, `SemanticSearch`) with registry selection, deterministic static baselines (rule classifier, lexical similarity), and a per-capability eval harness — no embedding model or vector store yet. Boundary and reversibility rules in [ADR 0035](adr/0035-two-layer-ai-and-local-interpretative-layer.md).
- `v0.40.0` — Typed ESPI event classification: official ESPI/EBI filings classified into typed `company_signals` (insider transactions, dividends, profit warnings, significant contracts, own-share transactions, guidance changes, general meetings) as the first consumer of the `Classifier` capability, with a deterministic rule classifier at ingestion plus an opt-in AI fallback that requires confirmation, and feed badge/filter, research-timeline, digest, and reminder surfacing. Calendar-event derivation for dated categories was deferred to `v0.41.0`. Design in [ADR 0034](adr/0034-espi-event-classification.md).
- `v0.41.0` — Report document ingestion & history backfill: ESPI/EBI report attachments persisted as stored report documents (full file for periodic reports, metadata + URL for other filings), an explicit on-track ~3-year history backfill of reports and filings, and derived dividend/general-meeting calendar events (deterministic-first body date parse with an opt-in AI fallback, always confirm-before-create). All target the active Bankier official-report path. Design in [ADR 0036](adr/0036-report-document-storage-and-backfill.md).
- `v0.42.0` — Management claims tracker: management claims promoted to a first-class entity with a due period and a user-set verdict; AI claim extraction (mandatory confirmation) from report documents and transcripts; a due-period derivation surfaces an open claim into a "claims to verify" review queue when the due-period report arrives, with KPI-backed verification for quantitative claims; claims flow into the research timeline, reminders/digests, and import/export. Design in [ADR 0040](adr/0040-management-claims-tracker.md).
- `v0.43.0` — Report-season cockpit: a time-driven Inbox-adjacent view of upcoming report dates across watchlists, each with a pre-report card composing open research questions, unresolved claims, last-period confirmed KPIs, and recent evidence, plus a prepare→process workflow. Backend-owned read models (no stored projection, no duplicated domain logic) over existing domains; the only new persisted state is per-occurrence preparation status (`report_preparations`). Design in [ADR 0044](adr/0044-report-season-cockpit.md).
- `v0.44.0` — Quality frameworks (quantitative checks): user-owned checklists of criteria in a free-text DSL over KPI metric keys, evaluated deterministically against confirmed fundamentals facts into a versioned, immutable-snapshot scorecard; ships an editable/clonable/resettable Kroeze-style template, the shared derived-metrics service (reused by `v0.54.0`/`v0.55.0`), a global `user` KPI scope, a broader computed-metric library, a user-facing `wiki/` with a DSL reference, and export/import of frameworks plus referenced custom metrics. Design in [ADR 0046](adr/0046-quality-frameworks-quantitative.md).
- `v0.45.0` — Interpretative AI layer (embedding model): on-device `candle` encoder (`intfloat/multilingual-e5-small`, optional one-time download, behind the off-by-default `embedding-model` feature) + a disposable `content_embeddings` vector store (pure-Rust brute-force cosine). Wires the model-backed `SimilarityProvider` only; a Polish ESPI/EBI eval (model top-1 ≈83% vs lexical ≈42%) gated adoption. Configurable in Settings → AI. Confirmed runtime defaults in [ADR 0035](adr/0035-two-layer-ai-and-local-interpretative-layer.md).

## Active And Upcoming Milestones

### Foundational (cross-cutting, lands next, ahead of `v0.46.0`)

- **Test architecture & coverage foundation** — a foundational, cross-cutting epic (not a product-capability milestone, so it carries no `milestone:vX.Y.0` label; it lands ahead of `v0.46.0`). Closes the pre-policy test-coverage debt found in the audit and builds the regression loop the full-coverage policy needs. Sequenced **keystone-first**: (1) a canonical, deterministic, per-test-isolated **sample-data factory** projected into both the Rust seed builder and a now-**stateful browser mock runtime**; (2) **layered parallelism** (`cargo nextest`, Playwright `fullyParallel`, a staged concurrent `make check`); (3) high-risk backend backfill (migration safety/idempotency + a historical-schema migration corpus, `restore_backup`, destructive feed cleanup); (4) **broad clickable Playwright journeys** across all 12 primary screens; (5) Vitest fill (`Watchlists`, `Diagnostics`, error/empty states) + remaining command contracts. The extended clickable suite is promoted toward a default/pre-merge gate once fast and stable. Design in [ADR 0048](adr/0048-test-architecture-sample-data-broad-clickable-coverage-and-layered-parallelism.md), amending [ADR 0021](adr/0021-browser-ui-regression-testing.md). **Status: completed in `v0.45.1`.**
  - **Test architecture v2 — data-transform correctness** (delivered): property/invariant tests, golden snapshots, parser fuzzing (stable), behavioral scale gates + periodic benches, the dual-execution mock-fidelity contract, and an e2e ingestion pipeline test — hardening the harness for the data-heavy roadmap before those epics land. Design in [ADR 0049](adr/0049-test-architecture-v2-data-transform-correctness.md). **Status: completed in `v0.45.1`.**
- **Failure-path & real-state test layer** (epic #40, planned 2026-07-29, [ADR 0091](adr/0091-failure-path-and-real-state-testing.md)) — a foundational, cross-cutting epic (no `milestone:` label): chaos seam reachable from browser specs, poor-state scenario walks, user-visible job-failure surfaces with an enumeration gate, the real-DB honesty ratchet (local, on-demand advisory) and a synthetic shape corpus for public CI. Six slices (#105, #74, #234–#237) delivered as commits on **one epic PR** (#239 — owner decision 2026-07-29: no binaries from a partially implemented epic); execution plan [docs/plans/failure-path-real-state.md](plans/failure-path-real-state.md). **Status: completed** (epic #40 closed 2026-08-05; delivered detail in [CHANGELOG.md](../CHANGELOG.md)).
- **Fundamentals — data trust** (epic #229, planned 2026-07-30) — a foundational, cross-cutting epic (no `milestone:` label): the "numbers in the live DB must be trustworthy" family — every known bad-data class gets repaired by a forward idempotent migration AND ships the guard that keeps its class out (ADR 0045). Seven tasks, one commit each on a single epic PR, measure-first: T1 real-DB trust audit (rates for every class) → T2 container truth end-to-end (magic-byte format persisted at store time, all extension-routing call sites rerouted, incl. the canonical structured tie-break) ∥ T4 currency integrity (ESEF divide-unit fix + ISO-or-null write guard + EPS repair) → T3 document-association trust (T-A3 guard hole, URL distrust for canonical rank, per-company coverage roll-up) → T5 BiznesRadar positive corroboration + durable divergence outcomes + flagged-fact reachability → T6 honest metric windows (quarterly TTM) / T7 kpi_relevance lifecycle (create-time core seed, study #81 → ADR, minimal #149). Execution plan: [docs/plans/fundamentals-data-trust.md](plans/fundamentals-data-trust.md). **Status: completed** (epic #229 closed; delivered detail in [CHANGELOG.md](../CHANGELOG.md)).
- **Architecture v2** (delivered) — a foundational, cross-cutting epic ([ADR 0050](adr/0050-architecture-v2-domain-stores-source-pipeline-durable-jobs.md)), the structural counterpart to the test-architecture work, made safe to attempt *by* it. Its mandate — build the **foundations** ahead of the features that need them — is delivered: (1) `AppState` decomposed into concrete **domain stores** (not a repository port — [ADR 0039](adr/0039-ports-and-adapters-posture.md) stands); (2) the **`SourceAdapter` port** realized (descriptor registry + registry-driven refresh dispatch); (3) the **ingestion pipeline spine + entity-resolution** module with a persisted cross-source **story key** (the clustering / compare enabler); (4) **frontend state decomposition** — every screen reads a per-domain view-model context. **Two forward consumers are carried to their milestones, not dropped:** the **Rust-side scheduler** enqueuing onto the durable queue (queue + worker delivered) lands with the **Autonomous report pipeline**, and the **ANN vector-index activation** on the persisted index (pure-Rust HNSW impl + swap boundary delivered, cross-build-validated) lands with **Cross-company KPI comparison** — both milestone-labelled + parented to those epics in Radicle. **Status: completed (foundations) in `v0.45.1`; AV5 (Rust-side scheduler) delivered in `v0.49.0`; AV6 (ANN activation) and the story-key persist path retired in `v0.52.0` after their consumers were dropped ([ADR 0080](adr/0080-retire-embedding-model.md)).**

Everything through `v0.61.0` is delivered (below, with its shipped version). The forward plan is **functional epics in dependency order** — no fixed version numbers; the version is set by each PR's `release:*` label on merge ([ADR 0090](adr/0090-github-canonical-forge-and-continuous-release.md)). Live status + IDs on the "Brawler board" (see [kanban.md](kanban.md)):

> **Re-sequenced 2026-07-03** after a full-codebase audit + product planning session with the owner (ADRs [0067](adr/0067-market-data-foundation.md)–[0074](adr/0074-ux-journeys-and-anti-rot.md)): the market-data → attention → sources → ownership block lands ahead of the valuation arc (which needs prices and sectors), judgment capture is pulled forward (history cannot be backfilled), and the notebook reinvention follows the UX-journeys work that informs it.

- `v0.46.0` — **Story clustering across sources** — **evaluated and dropped.** The cross-source dedup was implemented heuristic-first and validated against a real production database; no local method (lexical or `e5-small` embedding) reaches trustworthy precision at useful recall (best local result: precision 0.79 / recall 0.73 — ~1 in 5 merges wrong). It is also a nice-to-have at this app's real scale, not core. The only reliable path is an LLM judge, deferred to a future milestone behind the AI provider boundary. Rationale and real-data evidence in [ADR 0051](adr/0051-story-clustering-across-sources.md).
- `v0.47.0` — **Report-over-report diff (structured financial statements)**: a pure-Rust, deterministic section-level diff between two consecutive same-type financial statements (consolidated SSF, standalone JSF), reachable from the company workspace and on new-report arrival. **Scope narrowed after a real-data spike** ([ADR 0052](adr/0052-report-over-report-diff.md)): pure-Rust `pdf-extract` text extraction is reliable across issuer formats (alpha-ratio 0.80–0.89, Polish diacritics intact), and structured statements align 85–92% by heading with a deterministic self-diff = empty invariant — but the narrative management report (MD&A) aligns only 4% by heading. The **narrative MD&A diff and the cited AI delta summary are deferred** to a later milestone (needs stronger section detection + embedding-backed alignment). No AI-provider dependency this milestone; ships fully local/offline. Section alignment uses a heading + lexical baseline with the `v0.45.0` embedding model as an optional enhancer (never a hard dependency). **Status: completed in `v0.47.0`.** Market-wide extraction validation (770 companies → 613 reports, 89.4% GOOD, 0 silent-garbage) and the descope rationale are in [ADR 0052](adr/0052-report-over-report-diff.md); the richer presentation layer (inline value-aware table) and the AI delta summary are tracked deferred follow-ons (Radicle `289aac4`, `bd9ab19`).
- `v0.48.0` — **Research workspace shell (mode-based, thesis-centric, [ADR 0054](adr/0054-mode-based-thesis-centric-shell.md))**: the cross-cutting shell (below) shipped here as the headline minor — left-sidebar IA spine + pinned companies, the **Today/Pulse Triage attention home** (the new default landing), the sectioned **Company workspace** (its opt-in docking layout retired 2026-08-28, [ADR 0108](adr/0108-retire-docking-engine.md)), and full-screen **Focus** reader/writer modes. This **absorbs the planned feed-triage milestone's delivered parts** — the Triage-style attention home and the command palette (the `⌘K` palette). That milestone is reframed: its **remaining keyboard feed-triage state (accept/snooze/dismiss) and `SemanticSearch`** (find-by-meaning ranking over the embedding model) become a **deferred follow-on** that plugs into the Today home, re-slotted to a later version **without renumbering `v0.49.0`+** (`SemanticSearch` since retired with the embedding model — [ADR 0080](adr/0080-retire-embedding-model.md)). Per-company conviction status and the Compare KPI table are shell placeholders pending their dependency milestones (quality `v0.50.0` / valuation `v0.62.0` / thesis `v0.65.0` / cross-company `v0.61.0`). **Status: completed in `v0.48.0`.**
- **Cross-cutting — Research workspace shell (mode-based, thesis-centric)** **shipped as `v0.48.0`** (above) and continues to restructure the app incrementally through the milestones below; the epic itself spans versions (like the foundational epics above) — `v0.48.0` delivered the spine, Triage home, sectioned workspace and Focus modes; conviction status and Compare fill in with their dependency milestones. **Re-scoped 2026-06-23 ([ADR 0054](adr/0054-mode-based-thesis-centric-shell.md)):** after real owner use, the app-wide freeform grid ([ADR 0053](adr/0053-dockview-layout-pilot.md), engine retired 2026-08-28 by [ADR 0108](adr/0108-retire-docking-engine.md)) felt overwhelming and directionless for a non-professional investor; a cited UX research pass confirmed the fix. The shell becomes **mode-based** (Today/Pulse · Company workspace · Compare · Focus) with a **left-sidebar IA spine + pinned companies**, a **Triage-style attention home**, a **sectioned company deep-dive**, and a **glanceable per-company conviction status** (the opt-in docking layout that stayed inside the workspace mode was retired 2026-08-28, [ADR 0108](adr/0108-retire-docking-engine.md)). Full plan in the dedicated section below and [ADR 0054](adr/0054-mode-based-thesis-centric-shell.md) / [ADR 0053](adr/0053-dockview-layout-pilot.md).
- `v0.49.0` — **Autonomous report pipeline** (North Star, detailed below): detect publication, auto-fetch, auto-extract, diff, cross-reference, and notify, behind a per-company trust ladder. **Delivered ([ADR 0055](adr/0055-autonomous-report-pipeline-trust-ladder.md)):** a per-company two-rung ladder (`off` → `assist` = auto-work but facts stay `pending` → `autopilot` = auto-commit as `auto_unreviewed`); orchestration as **chained durable-queue jobs** stamped with one `autopilot_run` id; **event-driven detection** off source-refresh completion (closes the AV5 Rust-side scheduler, retiring the frontend refresh timer); a persisted `autopilot_run` record backing the single Today/Pulse notification, the review queue, and run-level undo. Global confirm-before-commit default unchanged; decision-support only. **Status: completed in `v0.49.0`** — shipped together with structured-first deterministic KPI extraction, drift surfacing and provider pools (the delivered [ADR 0061](adr/0061-deterministic-fundamentals-data-gathering.md) slices; the epic continues), worker-pool queue fairness ([ADR 0059](adr/0059-worker-pools-and-queue-fairness.md)), per-company settings ([ADR 0056](adr/0056-per-company-settings-surface.md)), the composable-views finish ([ADR 0057](adr/0057-composable-views-and-curated-dashboard.md) decision 5), and the localized run card with review/undo; run-now and run-detail surfaces are tracked deferred (Radicle `7062f7e`).
- `v0.50.0` — **Quality frameworks (qualitative assessment)**: agent-assessed criteria (moat, pricing power, recurring revenue, capital allocation) with citations, composed into the scorecard and re-evaluated by autopilot. **Scope extended 2026-07-05 (owner decision): phase 2 — UX overhaul**: the full UX journeys & anti-rot epic ([ADR 0074](adr/0074-ux-journeys-and-anti-rot.md), Radicle `44beb6e`) plus a normative UI design system (spacing/type scale, semantic color tokens, one formatting layer for dates/numbers — kills the dual-formatter class of bugs), panel density contracts with per-pane-width tests, toast+undo feedback, global command palette, a11y debt paydown, all-panels visual-regression baseline, heuristic audit remediation of every section, and functional redesign of Today/Cockpit/Compare (cockpit view-level company context; Compare stub decision). Execution plan: [docs/plans/v0.50-ux-overhaul.md](plans/v0.50-ux-overhaul.md). **Status: completed in `v0.50.0`** — closed on T7 real-company validation (rounds 1+2 on the owner's live Windows app via live-drive); the owner's extraction-trust verdict continues as the cross-cutting trusted-extraction epic ([ADR 0077](adr/0077-trusted-extraction-foundations.md), below).
- **Cross-cutting — Trusted extraction foundations** ([ADR 0077](adr/0077-trusted-extraction-foundations.md), shipped as `v0.51.0`, continues the [ADR 0061](adr/0061-deterministic-fundamentals-data-gathering.md) epic): the owner's post-v0.50 trust verdict turned into measured reliability — document taxonomy (`doc_kind` + canonical report per period), a fundamentals **coverage map** (what has data, what is missing, why), a **history sweep** chained from backfill (add company → Backfill → data appears without per-document clicking), a free-tier-only vision/OCR tier-4 fallback chosen by a numeric ground-truth spike (Gemini vs Mistral OCR; descoped if neither clears the bar), one validation regime for AI and deterministic facts (`validation_status='none'` retired), AI call budgets, and a recall/precision ratchet on real labeled reports. Execution plan: [docs/plans/trusted-extraction-foundations.md](plans/trusted-extraction-foundations.md). **Status: delivered — closing `v0.51.0`** (F0–F6; the vision spike chose free-tier **Mistral OCR** and added a positional `html_positional` tier (both since retired — [ADR 0084](adr/0084-retire-in-app-ai-layer.md)/[ADR 0095](adr/0095-retire-html-positional-tier.md)); shipped the coverage map, backfill-chained history sweep, per-company OCR profile, per-sweep AI budget, and review queue); delivered detail in [CHANGELOG.md](../CHANGELOG.md).
- `v0.52.0` — **Judgment capture + read-only MCP MVP + embedding-model retirement**: (1) **Judgment capture** ([ADR 0071](adr/0071-judgment-capture.md)) — the minimal decision journal (recorded buy/pass/keep-watching + rationale + evidence links — the early slice of ADR 0043's journal, pulled forward because judgment history cannot be backfilled) plus pre-report expectations with post-report expectation-vs-actual review; (2) **read-only MCP MVP** (NS1 slice 1, pulled forward by owner decision 2026-07-06 — [ADR 0078](adr/0078-mcp-external-surface.md)) — localhost HTTP transport in-app (off by default, bearer token in the keychain), a thin stdio MCP adapter, and four read tools (company dossier, research search, claims due, quality assessments); advisory gating unchanged; (3) **retire the local embedding model** and dead subsystems ([ADR 0080](adr/0080-retire-embedding-model.md)) — the e5-small model + vector scaffold (ADR 0035 reversibility exercised), the write-only `story_key` ingest path, and dead UI plumbing. **Status: completed in `v0.52.0`** — all three streams plus the pulled-in backlog cards delivered; closed on owner dogfooding against the live Windows app (two rounds, all findings fixed in-milestone) and a real MCP client (Claude in WSL over mirrored networking) reading a live dossier; delivered detail in [CHANGELOG.md](../CHANGELOG.md).
- **Cross-cutting — UX quality loop v2** (planned after the active `v0.52.0`
  worktree; Radicle `bd1a6af`, no `milestone:` label) — extends the delivered ADR
  0074 journey net and ADR 0076 design-system gates with pre-code experience
  contracts, composable hostile-content and controlled-async frontend scenarios,
  deterministic friction/discoverability contracts, visual contact sheets, earlier
  real-Windows exploratory checkpoints, and escaped-defect classification. The loop
  is pilot-first on J1/J2; no new universal DoD blocker lands before the owner adopts
  it after the pilot. Execution plan: [UX Quality Loop v2](plans/ux-quality-loop-v2.md).
  Q2 consumes the generic failure-path seam owned by existing epic `0db7a7a` rather
  than duplicating it. **Status: pilot delivered with `v0.53.0`** (Q0–Q9 infra + J1/J2
  scenario matrix; adopt-all verdict recorded, [ADR 0081](adr/0081-ux-quality-loop-v2.md)),
  shipped alongside the **Dashboard unification** (epic `c793ca1`: one company-scoped
  Dashboard with follow presets, Companies→Library) in the same release.
- `v0.53.0` — **Market data foundation** ([ADR 0067](adr/0067-market-data-foundation.md), source selection [ADR 0082](adr/0082-market-data-source-selection.md)): automatic EOD quotes with full-history backfill, company sector classification (registry-sourced + manual override), market cap and level-0 market ratios (P/E, P/BV, EV/EBITDA, dividend yield, FCF yield, 52-week stats, own-history percentile) via the derived-metrics engine, surfaced as a price-context section + Basic info panel. Unblocks the valuation arc, price alerts, and the future backtest/calibration north stars. **Source pivoted after live probes** (Stooq's automated CSV gained an anti-bot gate): **Yahoo Finance v8 primary** (keyless, GPW `.WA`, PLN); Twelve Data was chosen then removed the same day when a live smoke proved GPW quotes are paid-plan-only there ([ADR 0082](adr/0082-market-data-source-selection.md) amendment; free degraded fallback tracked for `v0.55.0`). **Status: completed in `v0.53.0`** — shipped with the dashboard redesign + UX-quality-loop pilot (below); ratios compute via a `coalesce()` fallback chain, all live-verified on the owner's real watchlist; delivered detail in [CHANGELOG.md](../CHANGELOG.md).
- `v0.54.0` — **Attention routing + morning briefing** ([ADR 0068](adr/0068-attention-routing-and-morning-briefing.md)): the `Toast` primitive (the persistent attention variant it added was retired by [ADR 0097](adr/0097-toasts-are-action-feedback-only.md) — toasts are action feedback only), user-owned alert rules over typed signals / autopilot events / price conditions, and a cited "what changed + what needs doing" morning briefing on Today. **Status: completed in `v0.54.0`** — alerts live as their own Library screen (owner IA decision, mockup-first redesign), rules evaluate inline at the three ingestion hooks (no new worker lane), the briefing is a routable AI capability with a no-provider structured fallback; all live-verified on the owner's real data (incl. a live-caught rule-id regression, fixed with content-derived ids + typed duplicate errors). Delivered detail in [CHANGELOG.md](../CHANGELOG.md).
- `v0.55.0` — **Source reliability & disclosure signals** ([ADR 0069](adr/0069-source-reliability-and-disclosure-signals.md)): the `Fetcher` trait on the SourceAdapter port, ESPI/EBI re-enabled as a reconciliation second witness (closes the Bankier single-point-of-failure), the KNF short-selling registry adapter, and the auditor-opinion/going-concern signal. **Status: completed in `v0.55.0`** — trait at the refresh level (ADR 0069 amended, owner-approved), all seven active adapters migrated; witness live with a pair ledger + system attention events (first measured completeness run: 15 witness items / 5 matched / 0 missed by Bankier); KNF via its stable public JSON endpoint with history, signal, alert-rule wiring and a company-view panel (the cockpit host was retired 2026-08-28, ADR 0108); `auditor_opinion` validated on the real database (0 false positives). All live-verified on the owner's real app; delivered detail in [CHANGELOG.md](../CHANGELOG.md).
- `v0.56.0` — **Ownership structure** ([ADR 0072](adr/0072-ownership-structure.md)): `ownership_stakes` with history (capital % vs votes % separately), extraction from already-stored periodic reports + the major-holdings ESPI signal + aggregator witness, holder-type classification (TFI/OFE/founder/family foundation/state…), derived free float, donut + stake-over-time charts. **Status: completed in `v0.56.0`** — mid-milestone pivot on real-data evidence (ADR 0072 amended): BiznesRadar promoted to the automatic breadth source (49/50 companies covered vs 9/50 from reports), reports+ESPI demoted to depth/freshness, witness direction reversed; disclosure-basis current state with holder-identity merging, four dogfooding rounds (incl. a live-caught aggregator-parser defect, fixed with table anchoring + plausibility gates + repair migration), all live-verified on the owner's real app. Delivered detail in [CHANGELOG.md](../CHANGELOG.md).
- `v0.57.0` — **Company health** ([ADR 0083](adr/0083-company-health-scores-and-red-flags.md), planned 2026-07-17): **Status: completed in `v0.57.0`** (owner sign-off 2026-07-18; delivered detail in [CHANGELOG.md](../CHANGELOG.md)). Piotroski F + Altman Z″ (EM variant, financials excluded) as a deterministic Rust score computer over confirmed facts, strict completeness (full inputs or an explicit `insufficient_data` breakdown), scalars referenceable in scorecard criteria (scoped ADR 0046 amendment); the parsed insider substrate (MAR art. 19 transactions + periodic-report management holdings, ground truth first) feeding a skin-in-the-game badge and 90d/12m insider sentiment; a red-flags panel (auditor red flag, report delay, fund exit, score deterioration, short spike) whose flags raise typed signals through existing alert rules; plus the v0.56 ownership OCR-residual follow-on (card `5171372`). **Beneish M deferred** (owner decision — needs receivables/SG&A/D&A extraction with uncertain coverage; slots in as a third score family later). Extraction gains `current_assets`/`current_liabilities`/`retained_earnings`/`long_term_debt`.
- `v0.58.0` — **Analyst recommendations** ([ADR 0073](adr/0073-analyst-recommendations-tracking.md)): attributed third-party ratings/target prices with revision history, consensus-shift signals, "vs target" context beside market data — tracking, never advice. Resolves the former Future Study (see below). **Status: completed in `0.58.0`** (also delivered: the espi-wdf tier-0 spike verdict → ADR 0061 amendment + card `76a4636`; automatic feed retention disabled → card `cc674d4`).
- `v0.59.0` — **AI-layer retirement + 100% deterministic fundamentals** (scoped 2026-07-19/20, owner decisions; two epics — a deliberate exception to the one-epic-per-milestone convention, [kanban.md](kanban.md)): (1) **retire the in-app AI analysis layer — intelligence via MCP (BYOA)** ([ADR 0084](adr/0084-retire-in-app-ai-layer.md), epic `3579e69`): all 11 routed capabilities, the provider adapters/routing/pool, tier-4 OCR, and AI settings/credentials go (Gemini stays for transcripts only — data acquisition); deterministic ESPI classification and the deterministic briefing list remain; stored AI outputs are removed, not archived (migration `0102`, decision 5); product identity becomes "deterministic research substrate + MCP port"; (2) the remaining slices of the **100% deterministic fundamentals data gathering** epic ([ADR 0061](adr/0061-deterministic-fundamentals-data-gathering.md), epic `971aff6` — previously cross-cutting/unslotted, see below): the EspiCoverNote tier 2a Rust port, flagged-outcome provenance + review surface, the BiznesRadar "wyniki finansowe" live witness (source-ADR gated), and the real-data recall/precision harness. Execution plan: [docs/plans/v0.59-ai-retirement-deterministic-fundamentals.md](plans/v0.59-ai-retirement-deterministic-fundamentals.md). **Extended a third time (2026-07-21, owner decision — Track C, [ADR 0086](adr/0086-aggregator-primary-fundamentals.md)):** BiznesRadar promoted witness → **primary** core-KPI source (three report pages, every period column), the deterministic PDF fact arm retired (ESEF + WDF stay; PDF text survives for period derivation and insider attachments), facts become **review-free** (no ratification queue), one-off wipe + rebuild of all facts, and a general dead-code de-bloat sweep. The in-app report viewer drafted with Track C was deferred to a future milestone (2026-07-22, see Future section). **Status: completed in `0.59.0`.**
- `v0.60.0` — **Today re-invention + MCP surface v2** (planned 2026-07-22, owner decisions; two epics — a deliberate exception to the one-epic-per-milestone convention, [kanban.md](kanban.md)): (1) **Re-invent the Today view** (epic `4f6ec2c`, [ADR 0087](adr/0087-today-attention-home-v2.md)): the Today/Pulse triage home redesigned against journey J1 in [ux-journeys.md](ux-journeys.md), driven by the dogfooding study card `abd456e` — grouped/deduped/severity-ranked stream (typed three-level severity mapped in one backend place), toast-as-pointer policy (superseded by [ADR 0097](adr/0097-toasts-are-action-feedback-only.md): attention events no longer raise toasts at all), the briefing translation seam cut on the autopilot token pattern (closes ADR 0084 dec. 6), config-state banner. Runs as the **full ADR 0081 UX-quality-loop pilot** (experience contract + storyboard → owner-approved mockup → implementation); the J1 budget (≤15 interactions at 10 new items) is the acceptance bar. (2) **MCP surface v2** (epic `dc04eef`, [ADR 0088](adr/0088-mcp-surface-v2-ui-parity.md); NS1 slice folded in from `v0.61.0`, owner 2026-07-22): **UI-parity, registry-driven tool surface** over the typed command layer (schemars-generated schemas, frozen tools/list snapshot; every command classified or the gate reddens), capability tiers `read`/`act`/`excluded` (`mcpWritesEnabled` default OFF; **provenance-mandatory writes**; job triggers allowed, deletes/undo/settings UI-only), unclassified-filings triage tool pair, wiki how-to + the `brawler-mcp` agent skill with a catalog-drift gate, and the **MCP dogfooding ritual** as a closure step. Delivered beyond plan (owner dogfooding rounds): severity aging + systemic-cause cross-company aggregates with group Dismiss-all, evidence-specific row copy with fire-time title snapshots (migration 0114), filename-as-metadata rendering, in-app severity legend, alert-origin indicator, the attention **Archive** view, and the "UI dogfooding finding ⇒ overlay" gate rule. **Status: completed in `0.60.0`.**
- **Re-invent the notebook panel** (epic `ebbcb29`, informed by [ux-journeys.md](ux-journeys.md)) — **unscheduled** (dropped from `v0.59.0` 2026-07-19, owner decision; re-slot when taken up).

### Active program: MCP-native KPI acquisition (E1–E5, planned 2026-08-10)

> **Owner direction (2026-08-09/10):** the search for a "good-enough" global fundamentals API is over — no vendor covers GPW + NewConnect quarterlies with trustworthy provenance at an individual price. Brawler builds its own acquisition system instead: **the issuer's report stays the source of truth, a rich LLM (BYOA, over MCP) is the reader/operator, and Brawler guarantees state, provenance, validation, idempotency and atomic commit.** BiznesRadar remains a witness/complement, not the completeness foundation — the program's ADR — [ADR 0098](adr/0098-mcp-native-kpi-acquisition-lifecycle.md) — supersedes **on completion** the "aggregator primary / agent additive" fragment of [ADR 0086](adr/0086-aggregator-primary-fundamentals.md) and resolves the [ADR 0093](adr/0093-agent-acquisition-tier-and-preliminary-lifecycle.md) status drift. Live status on the board.

Epics in dependency order (E1 → E2 → E3; E4 starts once E1's manifest contract exists; E5 is an independent modernization track whose spike may run early):

- **E1 — Transactional KPI ingestion core** (#352, critical): durable `kpi_ingest_runs` (one run = one document/company/profile), staged observations distinct from canonical facts, a run state machine, deterministic manifest validation, and an **atomic, idempotent, whole-report commit** — replacing today's fact-by-fact writes. Closure includes a real re-ingest on the owner's Windows app.
- **E2 — Agent-first KPI acquisition over MCP** (#353): ~8 workflow tools (start/list/context/stage/validate/commit/status/cancel) with a compact ingest-context read model, typed repair results, an acquisition-scoped credential, bounded response budgets, versioned extraction profiles and the rewritten agent skill. Low-level MCP stays for manual repair; normal ingest goes only through the workflow.
- **E3 — Automatic GPW and NewConnect acquisition queue** (#354): issuer-universe registry, durable publication watermarks, a planner turning qualifying publications into exactly one run each, leasing/fairness on the existing durable job queue, progress/failure surfaces, the NewConnect backfill (#253) and the **binary Notoria RFI** (qualified-or-rejected; qualified data still enters via the same staging/validation pipeline).
- **E4 — KPI ingest conformance suite** (#355): golden manifests over a stratified, independently-labeled GPW/NC corpus, blinded ground truth, fresh end-to-end runs into an empty DB, deterministic replay fixtures as CI gates, transactional chaos tests, citation/missing-reason validation and token-economics ratchets; shares corpus registry and GT format with #331 without duplicating its deterministic-parser estimand.
- **E5 — MCP protocol modernization** (#356): current-spec lifecycle, paginated/cacheable auth-scoped discovery, output schemas (ending the text+structuredContent duplication), resources for large payloads, optional Tasks over the domain runs; a bounded rmcp spike feeds a binary SDK-vs-hand-rolled ADR. Does not block E1–E3.

### Cross-cutting UX epic: Research workspace shell (mode-based, thesis-centric)

> **Status: the first wave shipped in `v0.48.0`** — left-sidebar IA spine + pinned companies, the Today/Pulse Triage home (new default landing), the sectioned Company workspace and Focus reader/writer modes ([ADR 0054](adr/0054-mode-based-thesis-centric-shell.md)). Per-company conviction status remains a shell placeholder pending its dependency milestones (quality `v0.50.0` / valuation `v0.62.0` / thesis `v0.65.0`); the Compare mode shipped in `v0.61.0` and was removed 2026-08-10 (#351, ADR 0089 amendment); the feed-triage keyboard state is a deferred follow-on (`SemanticSearch` retired — [ADR 0080](adr/0080-retire-embedding-model.md)); the docking layout was retired 2026-08-28 ([ADR 0108](adr/0108-retire-docking-engine.md)).

> **Re-scoped 2026-06-23 — [ADR 0054](adr/0054-mode-based-thesis-centric-shell.md) (Accepted).** Real owner use showed an app-wide freeform grid overwhelms a non-professional investor; the fix: a **left-sidebar IA spine + pinned companies**, a **Triage attention home**, **sectioned company deep-dives**, and a **glanceable conviction status**. Build order: sidebar spine → Triage home → sectioned company workspace → conviction status → Focus (Compare shipped and was later removed, #351).

- **Research cockpit (docking engine)** — retired 2026-08-28 ([ADR 0108](adr/0108-retire-docking-engine.md)); the company surface is the engine-free Spółka screen ([ADR 0107](adr/0107-company-view-paradigm.md)). History: ADR 0053/0057.

### Valuation & decision arc (decision-making augmentor)

These milestones turn the fundamentals substrate into computed valuation and a decision workflow, so the app provides sourced facts **and** computed analysis as decision support. Valuation is delivered as **triangulation in layers** ([ADR 0041](adr/0041-deterministic-valuation-engine.md) update): level-0 market ratios ship early with `v0.53.0`, level-1 comparative valuation ships with Compare, the DCF engine ships last — several methods yielding a fair-value **range with a confidence grade**, never a single oracular number. Ordering below was finalized at the 2026-07-03 planning approval (the earlier provisional numbers shifted by the audit/brainstorm block above), shifted +2 at the 2026-07-20 re-sequencing (ADR 0084), then −1 at the 2026-07-22 `v0.60.0` planning (MCP surface v2 folded into `v0.60.0`, [ADR 0088](adr/0088-mcp-surface-v2-ui-parity.md)).

- `v0.61.0` — **Cross-company comparison + comparative valuation (level 1)**: side-by-side tables and multi-series trend charts comparing the same canonical KPI across watchlist peers — a comparison read model with period alignment and unit/currency normalization, every value linked to its source evidence — plus peer-multiples fair value, sector percentiles, and the **confidence grade** (data completeness, peer depth, method convergence, ADR 0061 validation states) with a football-field readout. Builds the real Compare mode (shell placeholder until then). **Planning complete 2026-07-27 — [ADR 0089](adr/0089-cross-company-comparison-and-valuation-l1.md)** (two epics: comparison / valuation L1; NBP FX layer + `fx_rates`; `valuation_runs` from v0.61; Porównaj as its own sidebar entry under Dashboard; approved storyboard `docs/mockups/v061-compare-storyboard.html`). Depends on facts (`v0.37.0`) + market data (`v0.53.0`) + sectors. ****Status: completed in `v0.61.0`;** the Compare screen was later removed (#351, ADR 0089 amendment) — the read models and the Fundamentals periods × deltas table remain.**
- **Deterministic valuation engine** (epic): a pure-Rust, deterministic valuation slice over confirmed `financial_facts` — DCF/owner-earnings (default for thin GPW peer sets), multiple-based bear/base/bull scenarios where peer sets are deep, an FCF normalization cross-check, peer-relative multiples with a thin-flag, and what-if/sensitivity. Outputs scenario fair values and — with `v0.53.0` prices — real upside, decision-support framed. Design in [ADR 0041](adr/0041-deterministic-valuation-engine.md).
- **Valuation-aware scoring + advisory-verdict seam** (epic): extend the `v0.44.0` versioned scorecard with a valuation dimension and a scenario/upside readout, and define the open-core `AdvisoryVerdictProvider` port with an **empty default** (decision-support only; no prescriptive output in the open-core build). Design and the open-core boundary in [ADR 0042](adr/0042-advisory-verdict-port-and-open-core-boundary.md). Post-ADR-0084 note: the qualitative dimension consumes **manual or MCP-written** criteria verdicts (no in-app AI assessor).
- **Watchlist command center** (epic): batch-run the valuation engine and scorecard across the watchlist into a ranked screener/leaderboard with deltas since the last run, a **heatmap** (companies × valuation/quality/signals/conviction/research freshness), and a **research-gap detector** ("tracked for 4 months, no notes and no thesis"); extends the report-season cockpit (`v0.43.0`) and comparison (`v0.61.0`). ADR at milestone planning.
- **Investment thesis workbench** (epic): persisted, provenance-stamped theses (verdict as decision support, scenario forecasts, variant/inversion/disclosed-gaps, valuation↔thesis link with orphan check) extending the already-accumulating `v0.52.0` decision journal with outcome review and thesis links. Design in [ADR 0043](adr/0043-investment-thesis-and-decision-journal.md).
- **Living thesis (newsfeed-as-input)** (epic): link feed items, report documents, signals, and events to theses for staleness state, what-changed diffs, catalyst-aware refresh, and re-score triggers — the differentiator that keeps a thesis fresh from the live feed.
- **Investor week calendar** (epic): a weekly working-day digest (Mon–Fri) extending the Company Events Calendar with composable, opt-in **layers** — company events (reports, IPO debut, ex-dividend), an opt-in **whole-market** scope toggle (untracked GPW tickers via a relaxed Bankier ingest), a **macro** lane (CPI/PMI/payrolls — model + manual + sample now, **live source deferred to a follow-up ADR**), and **market holidays** (curated GPW/US static dataset, `WOLNE` badge). Watchlist-first by default; foreign earnings out of scope. Inspired by the Koomberg weekly investor calendar. Design in [ADR 0058](adr/0058-investor-week-calendar.md).
- **Import/export v2 + offsite backup** (epic; deferred here from the pre-v0.52 plan at the 2026-07-11 planning pass — **schema stability first**: versioned bundle sections built while the schema is still growing (judgment tables in `v0.52.0`, market data in `v0.53.0`+) would need reworking every milestone): unified data bundle and per-feature coverage, including the financial facts + KPI definitions export/import deferred from `v0.37.0` and the `v0.52.0` judgment entities, plus rotating backups to a user-chosen offsite folder ([ADR 0032](adr/0032-search-and-backup-boundaries.md) update). Bundle-v2 ADR at take-up; execution plan parked in [docs/plans/v0.68-import-export-v2-offsite-backup.md](plans/v0.68-import-export-v2-offsite-backup.md).

Deferred follow-ons: the **SEC EDGAR XBRL fundamentals adapter** is promoted to north-star **NS4** (see [North Star: Talk To Your Research](#north-star-talk-to-your-research-mcp)); per-source analyst recommendations are now scheduled (`v0.58.0`, [ADR 0073](adr/0073-analyst-recommendations-tracking.md)), while a paid/restricted **aggregate analyst-consensus adapter** stays deferred (behind a flag + ADR).

**Cross-cutting — 100% deterministic fundamentals data gathering** ([ADR 0061](adr/0061-deterministic-fundamentals-data-gathering.md), epic `971aff6`) — completed, executed in `v0.59.0`. Owner requirement: **100% deterministic, 100% automatic** KPI gathering, no human-in-the-loop as steady state — a KPI is taken from the highest available tier, and every fact is validated or flagged, never silently wrong. The current ladder is **ESEF/iXBRL → ESPI cover-note (WDF) → BiznesRadar-primary** (review-free facts); the deterministic PDF parser and the positional `html_positional` tier were both retired along the way ([ADR 0086](adr/0086-aggregator-primary-fundamentals.md) / [ADR 0095](adr/0095-retire-html-positional-tier.md)).

Sequencing notes: any model-backed capability is adopted only where a per-capability eval beats the static baseline, keeping state reversible to static ([ADR 0035](adr/0035-two-layer-ai-and-local-interpretative-layer.md)) — the rule the embedding model was actually held to and failed, leading to its retirement ([ADR 0080](adr/0080-retire-embedding-model.md)). The quality-frameworks milestones (`v0.44.0` quantitative, `v0.50.0` qualitative) depend only on the fundamentals facts and are resequenceable. The fundamentals schema was validated against ~37 GPW companies across sectors; findings (statement-type packs, generalized unit model, fact variants, period model) are recorded in [ADR 0027](adr/0027-company-fundamentals-scope.md). The valuation & decision arc (`v0.61.0`+) depends on facts plus the `v0.53.0` market-data foundation (prices, sectors) and is GPW-first/DCF-lean for thin-market reliability; level-0 ratios land early with `v0.53.0` per the ADR 0041 layering update. The arc stays open-core and decision-support only: any prescriptive (buy/sell/hold) advisory output is supplied by an out-of-band adapter behind the `AdvisoryVerdictProvider` port and is absent from the open-core build, preserving the `CLAUDE.md` decision-support rule and the planned recommendation-guardrail enforcement — see [ADR 0042](adr/0042-advisory-verdict-port-and-open-core-boundary.md).

## North Star: Talk To Your Research (MCP)

With the autonomous report pipeline delivered (`v0.49.0`, below), the guiding star becomes **"talk to your research"**: a local MCP server exposing Brawler's domain (facts, quotes, valuations, scorecards, theses, notes, signals, journal) as strict typed tools, plus a cited conversational surface over the user's **own** research — AI about *my* accumulated work on a company, not generic AI about a ticker. The design posture recorded in [Future Exploration: Agent / MCP Surface](#future-exploration-agent--mcp-surface) (local-only, second inbound adapter over the same typed contracts, deterministic engine, advisory gating unchanged) stands; this section promotes it from exploration to the destination arc.

North-star arc (provisional order; each item gets its ADR when taken up):

1. **NS1 — MCP surface + research chat**. The main north star. **Slice 1 pulled forward (owner decision 2026-07-06): the read-only MCP MVP ships in `v0.52.0`** ([ADR 0078](adr/0078-mcp-external-surface.md)) — localhost HTTP transport in-app, thin stdio adapter, four read tools. **Slice 2 pulled forward (owner decision 2026-07-20, ADR 0084) and folded into `v0.60.0` (owner 2026-07-22, [ADR 0088](adr/0088-mcp-surface-v2-ui-parity.md)): MCP surface v2** — UI-parity registry-driven tool coverage, write tools with mandatory provenance, and the unclassified-filings triage tool; after the AI-layer retirement the MCP port is the app's sole intelligence channel, so its quality is measured like the UI's (MCP dogfooding ritual at closure). The conversational surface (research chat) stays a later slice (after `v0.65.0` — the chat needs the thesis/valuation domain to exist).
2. **NS2 — Investor calibration loop**: decision journal + pre-report expectations + outcomes + price history composed into a "where am I right / where am I wrong" review. Data accumulates from `v0.52.0` (judgment) and `v0.53.0` (price history).
3. **NS3 — Framework backtest**: did high-scorecard companies actually behave better? Honest verification of the user's own criteria against price history.
4. **NS4 — Multi-market**: the SEC EDGAR XBRL fundamentals adapter (new `financial_data` source type; promoted from the deferred follow-ons), after GPW support is truly closed out.

Anti-goal, recorded deliberately: **position sizing / allocation suggestions / Kelly-style output is advice and stays permanently outside the open core** (see Not In V1 and [ADR 0042](adr/0042-advisory-verdict-port-and-open-core-boundary.md)).

## Delivered North Star: Autonomous Report Pipeline (v0.49.0)

The fundamentals, extraction, diff, claims, and company-view milestones are building blocks toward one experience: a tracked company publishes a periodic report, and the app detects it, fetches it, extracts the figures, summarizes what changed, cross-references the result against open claims, research questions, and evidence, and surfaces a single notification — with no manual steps.

This is deliberately sequenced last (v0.49.0) because it composes everything before it. It introduces a trust ladder rather than changing the confirmation guarantee: confirm-before-commit stays the default, the user opts a specific company into auto-confirm, and auto-committed facts carry a distinct unreviewed provenance state so they stay flagged, reversible, and cited. The financial_facts confirmation model in v0.34.0 is designed so this state is an additive value, not a later migration.

Boundary: fetching and analyzing while the app is closed crosses into a hosted/scheduled service and belongs to the managed-AI paid frontier, not the open core. Autopilot runs while the app is open.

**Status: completed in `v0.49.0`.** Delivered history: [CHANGELOG.md](../CHANGELOG.md); completed-card detail: [Kanban Archive](kanban-archive.md).

## Future: Release Packaging And Distribution Hardening

Goal: harden distribution after the first public Linux/Windows release artifacts are proven.

Candidate scope: Windows installer packaging; app version/about screen; optional tag-driven release-candidate workflow; native Pacman packaging if AppImage isn't enough for Arch; code signing; package repositories.

Not scheduled.

## Future: Full Backup And Restore

Goal: full local backup/restore beyond the scoped M20 import/export feature.

Candidate scope: full app-data backup format; restore safety while the app is running; exclusions for secrets, license tokens, logs, diagnostics, metrics, private signing material; cross-version compatibility strategy; manual recovery docs.

Not scheduled.

## Future: In-App Report Viewer

Goal: read stored report PDFs inside the Brawler window (dockable panel, WebView2 rendering, `#page=N` deep links from fact provenance), so a filing can be read next to the numbers it produced.

Deferred out of `v0.59.0` (owner, 2026-07-22 — first mockup rejected; design restarts mockup-first per [ui-authoring.md](ui-authoring.md)). Until then stored PDFs open externally. Not scheduled.

## Future: Windows Taskbar Unread Indicator

Goal: make unread Inbox activity visible from the Windows taskbar without opening the app.

Candidate scope: dot-style taskbar indicator when unread feed items exist, cleared at zero; unread state routed through a small desktop taskbar indicator boundary; Windows adapter for the real integration; no-op adapter elsewhere; native Windows packaged-app smoke test for appearance/clearing.

Deferred: numeric unread badges; source-failure/license/background-job indicators; non-Windows taskbar behavior.

Architecture: Inbox publishes unread activity state, not Windows taskbar API calls directly. The desktop boundary must allow later platform adapters/richer attention states without changing Inbox behavior. If Tauri lacks a suitable Windows taskbar overlay/badge API, isolate a Windows-specific native adapter behind the same boundary.

Not scheduled.

## Future Exploration: Terminal Interface

Goal: explore a keyboard-first terminal version after the desktop v1 foundations are stable.

Intent:

- provide a dense TUI experience loosely inspired by `k9s`
- reuse the same local domain/storage contracts as the desktop app
- use the night-neon visual identity in terminal-safe colors
- support fast feed, watchlist, company, and notebook navigation
- make optional synthwave-style background music an explicit opt-in experiment

Not in scope for v1.

## Future Exploration: Mobile And Sync

Goal: explore mobile clients and cross-device sync after local-first desktop workflows are proven.

Intent:

- provide access to watchlists, inbox, notes, claims, and transcripts on mobile devices
- preserve offline-first behavior where practical
- design sync ownership/hosting, encryption, conflict resolution, account model, and privacy guarantees before implementation — plus the product/distribution implications (stores, updates, platform policies)

Not in scope for v1. Cloud backup/sync remains a separate design discussion.

## Resolved Study: Google Finance Source Value

Resolved 2026-07-29 (owner decision, issue #107): **product-reference only, never a source.** Google Finance exposes no official public data API (deprecated 2012), so any ingest path would be scraping, which the source policy rejects. Its UX (watchlist-centric company pages, news grouping) may inform future design work without a standing card.

## Resolved Study: Analyst Recommendations Usage

The owner study (2026-07-02) is **resolved 2026-07-03** per its own decision criteria: promoted to milestone `v0.58.0` with design, entity shape, source-policy approach, and the tracking-never-advice presentation recorded in [ADR 0073](adr/0073-analyst-recommendations-tracking.md).

## Open Source-Strategy Questions

Genuinely unresolved questions carried over from [Source Strategy](source-strategy.md) (resolved/superseded ones moved to [Kanban Archive](kanban-archive.md#archived-investigation-and-study-notes-moved-2026-07-02); resolved 2026-07-03: Stooq EOD quotes → [ADR 0067](adr/0067-market-data-foundation.md), Bankier↔ESPI reconciliation → [ADR 0069](adr/0069-source-reliability-and-disclosure-signals.md)):

- Are there explicit GPW terms that constrain automated polling of `https://www.gpw.pl/komunikaty`? Never formally reviewed.
- ~~Which Investing.com Poland RSS categories are relevant to GPW companies?~~ **Resolved 2026-07-29 (#73): rejected for ingestion** — terms prohibit storing site data without written consent, and GPW signal is incidental-only; see [Source Strategy](source-strategy.md).
- What exact Portal Analiz scope is acceptable for v1? **Updated 2026-07-29 (#116): blocked on explicit publisher permission** — robots.txt bans bots (incl. `/feed/` for all agents) and Cloudflare 403-walls automated access; `portal-analiz` stays a disabled placeholder ([ADR 0014](adr/0014-portal-analiz-authenticated-source-policy.md)) until the owner obtains written permission; see [Source Strategy](source-strategy.md).

## Future: AI Recommendation Guardrail Enforcement

Goal: automated post-generation validation that detects and rejects AI output containing buy/sell/hold, portfolio allocation, or similarly actionable recommendation language.

M13 keeps the source-grounded prompt policy and decision-support UI positioning; hard output enforcement is explicitly deferred until after v1.

Not in scope for v1.

## Future Exploration: Agent / MCP Surface

Goal: expose Brawler's local domain (fundamentals facts, KPIs, valuation runs, scorecards, theses, signals) to an agent via a **local MCP server**, so conversational analysis can run over the same data the GUI uses — without making Brawler agent-native or leaving local-first.

Intent:

- A second **inbound adapter** over the existing domain core (alongside the UI ↔ Rust typed-command seam), reusing the same typed contracts, per the ports-and-adapters posture ([ADR 0039](adr/0039-ports-and-adapters-posture.md)).
- **Local-only** (stdio/loopback), no cloud/data egress; strict typed tools mirroring the existing command surface (no arbitrary shell or broad filesystem access).
- **Engine stays deterministic** ([ADR 0041](adr/0041-deterministic-valuation-engine.md)): the agent orchestrates/synthesizes, valuation/scoring stay deterministic Rust — agent-as-core-engine is a deliberate non-goal.
- Inherits the open-core advisory-gating boundary ([ADR 0042](adr/0042-advisory-verdict-port-and-open-core-boundary.md)) unchanged.

Sequencing: value only once the valuation & decision arc domain exists — a follow-on, not part of it. Record surface design and tool contracts in an ADR before implementation.

**Promoted 2026-07-03 to the destination arc as NS1 — see [North Star: Talk To Your Research](#north-star-talk-to-your-research-mcp).** The design posture above stands. **Slice 1 (read-only MCP MVP) was pulled forward to `v0.52.0` with its surface ADR ([ADR 0078](adr/0078-mcp-external-surface.md))**; the conversational surface keeps the original sequencing (after `v0.65.0`).

## Future: Cloud Backup And Sync

Cloud backup/sync is not part of core v1 implementation. It is a future roadmap area that requires a separate design discussion and ADR covering identity, encryption, sync conflicts, storage provider, monetization, and cost.

## Not In V1

- a global paid fundamentals API as the data foundation — evaluated 2026-08 (EODHD, StockWatch B2B, Bankier/Notoria upstream, and the ruled-out global vendors); no offer covers GPW + NewConnect quarterlies with trustworthy provenance at an individual price, so acquisition is built in-house (see [Active program: MCP-native KPI acquisition](#active-program-mcp-native-kpi-acquisition-e1e5-planned-2026-08-10)); the single Notoria RFI (#367) is the one remaining paid-source question
- sub-5% fund-position depth from BiznesRadar's "Pozostali akcjonariusze" table (fund-financial-statement data; the v0.56 aggregator basis deliberately ingests only the ≥5% "Główni akcjonariusze" disclosure picture — candidate for a later institutional-composition feature)
- portfolio position tracking
- position sizing, allocation suggestions, or Kelly-style output — advice, permanently outside the open core ([ADR 0042](adr/0042-advisory-verdict-port-and-open-core-boundary.md) posture)
- trade journal (note: a decision journal, distinct in scope, lands early at `v0.52.0` — see [ADR 0071](adr/0071-judgment-capture.md) / [ADR 0043](adr/0043-investment-thesis-and-decision-journal.md))
- billing/payment infrastructure
- hosted license activation
- multi-user / team accounts (single local user only)
- cloud sync and hosted data-service metadata — see [Future: Cloud Backup And Sync](#future-cloud-backup-and-sync)
