# v0.57.0 — Company health (SKELETON — do not implement from this file)

Epic: `<created 2026-07-03; see rad issue list>` · **ADR not yet written** — this milestone requires a planning session (owner + strongest model) before start, incorporating lessons from v0.50 (qualitative scorecard composition) and v0.56 (ownership feeds).

Goal: deterministic settled-science health scores + insider sentiment + a red-flags panel.

## Workstreams (to be expanded into tasks at planning)

- **Piotroski F / Altman Z / Beneish M** as deterministic derived metrics over confirmed facts, composed into the scorecard (extends ADR 0046). Open: which Altman variant for non-manufacturers/banks (sector-dependent — needs v0.53 sectors); input-completeness policy (score with gaps vs `insufficient_data`).
- **Insider sentiment**: aggregation over existing insider-transaction signals (who/how much/when, timeline + rolling net view). Open: window sizes, minimum-data threshold.
- **Red-flags panel**: composition of auditor signal (v0.55), going concern, report delays (needs a "expected vs actual publication date" source — verify calendar data suffices), fund exits (v0.56 stakes), score deteriorations. Open: flag severity model, dismissal/acknowledgement UX.
- Alert-rule triggers (v0.54) for new red flags; journey J1/J7 integration.

## Known constraints
- Decision-support framing throughout (scores are published formulas — cite them; no verdict language).
- All three scores need multi-period confirmed facts — behavior with thin history must be explicit, never silently wrong.
- Real-data validation: compute scores for maintainer-tracked companies and cross-check a sample against published values before default-on.
