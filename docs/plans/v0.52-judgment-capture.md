# v0.52.0 — Judgment capture + read-only MCP MVP + embedding-model retirement

Epics: judgment `3242a93` · NS1 slice 1 (parent `6ed8ad1`) · cleanup (ADR 0080 task) · Design: [ADR 0071](../adr/0071-judgment-capture.md) (Accepted) · [ADR 0078](../adr/0078-mcp-external-surface.md) · [ADR 0080](../adr/0080-retire-embedding-model.md) — **owner sign-off of 0078/0080 gates streams M and R**; stream J needs no gate.

Scope grown at the 2026-07-11 planning pass **in lieu of renumbering** (roadmap v0.52.0 entry is canonical). Import/export v2 + offsite backup deferred to `v0.67.0` (schema stability first). Card pool for filler slots: `c80dabe` (high, first) → `baba638` → `fd38d5a` → `02e991e` → `b97e349` → `619c8d9` → `a91d260`; folded: `db9a292` into R1, `b6b866f` into M4.

Conventions for every task: new commands return `CommandError` (ADR 0070, first delivery via E0); new migrations from **0067** (append-only); every new IPC command = fidelity-corpus step + `mock_fidelity.rs` arm + `runtime.ts` case + `make types`; new panels get a narrow-viewport overflow assertion; tests written first, red evidence pasted into the task card.

## Wave plan (max 3 concurrent delegated slices)

| Wave | Slices | Gate |
| --- | --- | --- |
| 0 | H1 housekeeping + M0 (ADR 0078) + R0 (ADR 0080) — orchestrator | owner ADR sign-off |
| 1 | E0 ∥ J1 ∥ C-card | — |
| 2 | J2 ∥ M2 ∥ C-card | ADR 0078 signed |
| 3 | J3 ∥ J4 ∥ M1 | — |
| 4 | J5 ∥ M3 ∥ C-card | — |
| 5 | M4 ∥ R1 ∥ C-card | ADR 0080 signed (R1) |
| 6 | M5 ∥ remaining C-cards (frontend-only) | — |
| 7 | Closure (orchestrator + owner) | owner sign-off |

## Stream E — foundation

### E0 — CommandError envelope (ADR 0070 first slice)
`commands/error.rs`: `CommandError { code, message }`, closed code enum, `From<StorageError>` mapping; ts-rs DTO; dual-shape rejection handling in the `src/api` invoke wrapper; contracts.md **Error codes** section. Tests that redden: nextest variant→code mapping (every `StorageError` variant covered), insta envelope snapshot, Vitest dual-shape wrapper, `make types-check`.

## Stream J — judgment capture (ADR 0071; shapes are STOP-AND-ASK)

Shapes (unchanged from ADR 0071): `decision_entries` — `company_id`, `kind: buy|pass|keep_watching|sell_note`, `rationale_md`, typed evidence links, `decided_at`, `superseded_by_entry_id`; **immutable once saved** (DB `BEFORE UPDATE/DELETE` triggers `RAISE(ABORT)`, no update command). `report_expectations` — occurrence key aligned with `report_preparations` (`company_id`, `event_key`) resolving to (fiscal year, period type); `stance_md`; optional metric rows (`metric_key`, comparator, expected value); editable **until** the period's facts are confirmed (freeze checked **inside the update transaction** against `facts_coverage_by_period`, `frozen_at` stamped on first observation); user's own `resolution_note_md` at review. Expectation-vs-actual = read model, no stored projection. Both entities owner-durable, retention-exempt; bundle export = explicit **v0.67** dependency.

### J1 — Migrations + storage
`0067_decision_entries.sql` (CHECK kind, superseded_by, immutability triggers), `0068_report_expectations.sql` (parent + metrics child, `UNIQUE` occurrence); stores `storage/decision_journal.rs` + `storage/report_expectations.rs` + AppState accessors; data-model.md. Tests: migration corpus/invariants, round-trips, **ordering by `decided_at` not `created_at`** with divergent seed (DoD §C), trigger rejects raw UPDATE/DELETE, freeze race test (facts confirm mid-flow), proptest comparator evaluator (total, lt/gte duality, no panic).

### J2 — Commands + DTOs + timeline join
Seven commands on `CommandError`: `create_decision_entry`, `list_decision_entries` (company/kind filters, global chronological), `create_report_expectation`, `update_report_expectation`, `list_report_expectations`, `expectation_review` (read model), `record_expectation_resolution`. Timeline: `EVIDENCE_TYPES += "decision_entry"` (`storage/research.rs`), UNION arm in the timeline query, `ResearchEvidenceType += DecisionEntry` (`api_ts_unions.rs`) — additive, **no migration** (verified: `evidence_links.from_type/to_type` unconstrained). Fidelity ×7; contracts.md + data-model.md in the same change. Tests: fidelity steps, `timeline_includes_decision_entries_in_date_order`, types-check.

### J3 — Journal UI
Cockpit section `decisionJournal` in the `CockpitScreen.tsx` registry (template: `companyNotebook` — union, `PINNED_KINDS`, label, `renderPinned`); global chronological journal panel; composer (kind, Markdown rationale, evidence picker reusing the `ResearchEvidencePanel` link pattern); entries render in the company timeline; `text()` en+pl; ui-flows + ui-information-architecture. Tests: Vitest workflow (create→visible in section/global list; **no edit/delete affordance** asserted; supersede creates follow-up; empty state), Playwright narrow-viewport spec, a11y on the composer.

### J4 — Expectations in the report-season flow
"Write expectations" in the `.report-season-card-prep` block (`ReportSeasonScreen.tsx` + `useReportSeason.ts`): stance + optional metric rows with a picker fed by `PreReportKpi`; post-confirm review surface (expectation vs actual side-by-side + resolution note); autopilot `stage_cross_reference` adds an expectations cross-ref and `compose_summary` mentions the review when expectations existed. Tests: Vitest workflow (stance-only, metric row via picker, frozen read-only, review + resolution), nextest autopilot cross-ref with/without expectations, narrow-viewport.

### J5 — Journeys + budgets
J4 spec gains the expectations step; **J4 ceiling re-baselined in ux-journeys.md 10→~13** (owner decision — 6 of 9 current interactions are panel-opening overhead; the step is new value, not regression). J6 becomes the real recording flow (ceiling 15 has room). `budgets.json` floors + `ux-journeys.md` updated together after first measurement.

### J6 — Closure (stream part; full closure = Wave 7)
Docs closure, retro UX section, owner real use (≥1 upcoming report with recorded expectations + ≥1 journal entry) before sign-off.

## Stream M — read-only MCP MVP (ADR 0078; contracts are STOP-AND-ASK)

Architecture (locked at planning): second driving adapter over existing `AppState` read models (ADR 0039); stateless MCP Streamable HTTP subset — `POST /mcp` on `127.0.0.1:<port>` via **tiny_http** on a dedicated thread (no tokio feature changes); `GET`→405, notifications→202, batches rejected, 1 MiB body cap, Host-header validation; bearer token in keychain (`brawler/mcp/auth_token`), constant-time compare via SHA-256; off by default, app-open-only lifetime (spawned in the `lib.rs` setup closure beside `jobs::scheduler::spawn`); stdio adapter = dumb line→POST pipe (`src-tauri/src/bin/brawler-mcp-stdio.rs`, `reqwest::blocking`). Four read tools: `get_company_dossier`, `search_research`, `list_claims_due`, `get_quality_assessment`. Advisory gating unchanged (ADR 0042). rmcp re-evaluation trigger recorded in the ADR (full NS1).

### M1 — Settings + token
`McpSettings { enabled: false, port }` in `UserSettings`/`SettingsUpdate` (tolerant readers, no migration); `CredentialDescriptor` generalized beyond the AI-provider list; commands `regenerate_mcp_token` (plaintext exactly once), `revoke_mcp_token`, token status. Fidelity ×3. Tests: settings round-trip, descriptor, token generation (length/hex/uniqueness), fidelity, types-check.

### M2 — Core: protocol + tools + server
`src-tauri/src/mcp/{protocol,tools,server}.rs`: JSON-RPC dispatcher (`initialize` w/ version negotiation, `notifications/initialized`, `ping`, `tools/list`, `tools/call`), typed `deny_unknown_fields` inputs + hand-written JSON Schema constants, tiny_http server with auth. Tests: nextest protocol (−32601/−32700/−32602, batch-reject, notification-no-response), **insta `tools_list_schema.snap` = frozen tool contract** + `dossier_output.snap` on seeded in-memory DB, proptest dispatcher-never-panics + inputs-reject-unknown-fields, server integration on an ephemeral port (loopback-only, 401/403/405/413, token round-trip).

### M3 — Lifecycle
`McpServerHandle` in the setup closure; `set_mcp_enabled` (live start/stop), `mcp_status` → `{running, port, error}` (bind failure / missing token = status error, never a crash). Fidelity ×2. Tests: start/stop idempotent, rebind after stop, port-in-use→error, refuses without token.

### M4 — Settings UI (capability reachable)
Settings section "MCP server": enable toggle, port, token generate/copy(once)/revoke, status pill, Claude Code/Desktop connection snippet; declared journey-independent utility. Folded card `b6b866f`: stabilize the SettingsScreen theme-persist test. Tests: Vitest workflow (enable persists, plaintext once, revoke, error state), narrow-viewport.

### M5 — stdio adapter + docs + packaging
`brawler-mcp-stdio` bin; `wiki/mcp-server.md`; contracts.md planned tags removed (docs-drift enforces); `make package-windows-from-linux` **after R1** (verifies tiny_http + second bin + candle-free tree under cargo-xwin); **manual verification against a real client (Claude Code `--transport http`) is a named DoD step**. Tests: framing-fn unit, docs-drift.

## Stream R — retire embedding model + dead subsystems (ADR 0080)

### R1 — Removal (mostly deletions; after M1/M3 — touches `lib.rs`/settings)
- Model + scaffold: `embedding-model` feature + candle-core/nn/transformers + tokenizers + **instant-distance** out of Cargo.toml; `interpretation/{embedder,model,candle_embedder,vector,vector_index,embedding_similarity,eval}.rs` (**keep** `rule_classifier` — consumed by `storage/signals.rs`; keep `lexical_similarity` only if a live consumer remains after `find_similar_content` goes, else remove); `jobs/content_embedding.rs` + lane; `storage/embeddings.rs`; `commands/interpretation.rs` embedding commands + `api/interpretation.ts`; `EmbeddingSettings.tsx` + `EmbeddingModelSection.tsx`; `wiki/embedding-model.md`; model make targets/eval harness; testing.md + engineering-workflow §D example. Report-diff untouched (verified: no similarity use).
- story_key/entity_resolution: `entity_resolution.rs`, `derive_story_key` in `storage/ingestion.rs`, write sites in `storage/sources.rs`, golden/proptest suites.
- Small dead: `rename_cockpit_layout` (command+storage+api+DTO), OS pop-out path in `DockLayout.tsx` (button + Alt+P + `addPopoutGroup`; in-app floating stays) + `core:webview:allow-create-webview-window` capability.
- Forward migrations: `0069` DROP `content_embeddings` + purge queued `content_embedding` jobs; `0070` DROP COLUMN `feed_items.story_key` + index. `similarity_strategy` tolerant read maps `embedding`→`static`.
- Folded card `db9a292`: retire/repoint orphaned `AiCapability::KpiExtraction`.
- Guardrail (ADR 0045, same change): close the "orphaned `src/api` wrapper" class — scoped knip exports check for `src/api/**` (must not flag legitimate code).
- Tests: migrations idempotent + self-heal on a real old snapshot (with embeddings + story_key data), settings-read maps the old value, ingest works without story_key, suite green without the feature, knip clean (DoD §F), new guardrail reddens on a planted orphan.

## Tripwires (STOP-AND-ASK)

- Judgment: automated scoring of the user's judgment ⇒ STOP (mirror, never grade); thesis fields ⇒ STOP (v0.64); freeze outside the transaction ⇒ STOP.
- MCP: any mutating tool ⇒ STOP (new ADR); token in logs ⇒ STOP; bind beyond `127.0.0.1` ⇒ STOP; buy/sell/hold in any tool output ⇒ STOP (ADR 0042).
- R1: touching `rule_classifier` ⇒ STOP; touching the parked claim-extraction pipeline ⇒ STOP (owner decision, ADR 0080); destructive migration without a real-old-snapshot test ⇒ STOP; manual management-claims tracking and in-app floating panels stay untouched.
- Process: no commit/push unattended; failing gates are hard stops; one heavy build at a time (`CARGO_BUILD_JOBS=8`, scoped nextest).

## Closure (Wave 7)

Docs closure (product-spec, CHANGELOG scaffold, wiki), retro (both domains + UX section), spec-conformance audit of ADR 0071+0078+0080 decision-by-decision (live-path via `repoctx callers`), `make check-epic`, owner dogfooding (expectations on ≥1 real report + ≥1 journal entry + MCP queried by a real client), live-drive Windows (old DB with embeddings/story_key data opens cleanly), release via `brawler-release` on explicit owner sign-off only.
