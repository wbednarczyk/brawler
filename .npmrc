# Hard stop: refuse installs on a Node version the toolchain does not support
# (vite 8 / vitest 4 / stylelint 17 / knip require Node >= 20; nix pins 22).
# This turns the engines field into an enforced gate, not a silent warning.
engine-strict=true
