Verdict: **DO-NOT-SHIP the plan as written.** The direction is sound, but two S1 requirements are internally impossible and several contracts need tightening before integration. No files were edited.

1. **BLOCKER — #454’s required palette test cannot pass with the actual substring filter.** The brief says querying `open events` must return both `Open tool: Events` and `Open Events` ([s1-search-palette-feed.md:6](/home/wojtas/projects/brawler/docs/plans/wave-2026-09b-contracts/s1-search-palette-feed.md:6)). The palette uses contiguous `label.includes(query)` matching, so `open events` cannot match `Open tool: Events` ([CommandPalette.tsx:19](/home/wojtas/projects/brawler/src/shared/components/CommandPalette.tsx:19)). There are also three legitimate Events commands: shortcut `Open Events`, global `Open screen: Events`, and contextual `Open tool: Events` ([AppShell.tsx:143](/home/wojtas/projects/brawler/src/app/AppShell.tsx:143), [shortcuts.ts:52](/home/wojtas/projects/brawler/src/app/shortcuts.ts:52)). Correct the acceptance test to:

   - `open events` → exactly `Open Events`;
   - `screen: ev` → exactly `Open screen: Events`;
   - `tool: ev` → exactly `Open tool: Events`;
   - activating each lands on its intended surface.

   The `Open tool:` family is the right resolution. Hiding screen shortcuts only on Spółka would make the global accelerator context-dependent, contrary to [ui-information-architecture.md:16](/home/wojtas/projects/brawler/docs/ui-information-architecture.md:16), and would not address the separate `Open screen: Events` entry.

2. **BLOCKER — `Open notebook` cannot be retired globally.** S1 says to remove and pin the old `Open …` keys ([s1-search-palette-feed.md:6](/home/wojtas/projects/brawler/docs/plans/wave-2026-09b-contracts/s1-search-palette-feed.md:6)), but `text("Open notebook")` remains live in the transcript success strip ([TranscriptJobRow.tsx:361](/home/wojtas/projects/brawler/src/screens/Transcripts/TranscriptJobRow.tsx:361)). Adding it to `retiredKeys.test.ts` will fail its source scan; removing its PL entry will leak English. Preserve this key, or separately change the transcript destination to the noun `Notebook`/`Notatnik` and include that file and its tests in scope.

3. **HIGH — GlobalSearch must clear controller options during each asynchronous request.** While a new request is loading, the current component retains the previous `results` but renders only the loading status ([GlobalSearch.tsx:62](/home/wojtas/projects/brawler/src/app/GlobalSearch.tsx:62), [GlobalSearch.tsx:160](/home/wojtas/projects/brawler/src/app/GlobalSearch.tsx:160)). If those stale rows remain controller options, the input can expose an `aria-activedescendant` for an option absent from the DOM. It also prevents the requested reset when the new response contains the same active ID, because the controller deliberately preserves active identity across reordered option sets ([useComboboxListbox.ts:14](/home/wojtas/projects/brawler/src/ui/useComboboxListbox.ts:14), [useComboboxListbox.ts:47](/home/wojtas/projects/brawler/src/ui/useComboboxListbox.ts:47)). Specify `options = []` while searching, then the flattened settled response. Test response A → active shared ID → response B containing that same ID, and require response B’s first option to become active. Use a composite identity such as `contentType:sourceId`; `SearchMatch` has no standalone globally unique result ID.

4. **HIGH — the migration needs a visible keyboard-active state and a real input primitive.** With focus retained on the combobox input, `.global-search-result:hover, :focus-visible` does not paint the `aria-selected` option ([shell.css:351](/home/wojtas/projects/brawler/src/styles/shell.css:351)). The brief tests only ARIA, so an invisible keyboard selection can ship. Require the shared `.ui-combobox-option[aria-selected="true"]` skin or an equivalent paint assertion. Also, “keep the input markup” conflicts with the no-raw-control rule in [common.md:7](/home/wojtas/projects/brawler/docs/plans/wave-2026-09b-contracts/common.md:7): `SearchField` already supports refs, input props, clearing, and controller keyboard handling ([SearchField.tsx:16](/home/wojtas/projects/brawler/src/ui/SearchField.tsx:16)). Use it rather than preserving GlobalSearch’s obsolete raw-input exemption.

5. **HIGH — #451’s product empty state is unspecified and currently violates ADR 0104.** The existing empty state is only “No backups yet.” ([BackupsSection.tsx:139](/home/wojtas/projects/brawler/src/screens/Diagnostics/BackupsSection.tsx:139)); moving it into normal Settings makes the three-beat product empty-state contract applicable. Specify what backups are, that they are local, and one `Create backup` action. To keep the promised single primary, render the one create action in the empty state when empty and in the section toolbar when backups exist—not twice. The Settings inventory at [SettingsScreen.contract.test.tsx:221](/home/wojtas/projects/brawler/src/screens/Settings/SettingsScreen.contract.test.tsx:221) must enumerate `Create backup` (`create`), `Refresh` (`control`), and each visible `Restore` (`control`), including loaded and empty states.

6. **HIGH — #476’s interim assertion does not match either itself or PR #477’s helper contract.** The brief permits a sibling style as the “before” value but then demands a change on the same element ([s2-backups-paint.md:6](/home/wojtas/projects/brawler/docs/plans/wave-2026-09b-contracts/s2-backups-paint.md:6)); those are different proofs. A second document is sufficient for a neutral-sibling check, but not for the required same-row before/after proof. First open unmarked Documents, capture `doc_cdr_q3_2025`, then trigger the Activity deep link and recapture that logical row. Mirror PR #477 exactly—`backgroundColor`, combined `outline`, and `boxShadow`—rather than the brief’s `borderColor`, so integration can mechanically replace the inline code with `captureMarkStyle`/`expectVisiblyMarked`.

7. **HIGH — the Claims browser proof is missed because it uses a class, not an attribute.** The conditional wording “if a spec asserts its mark by attribute” misses [j1-morning-review.spec.ts:145](/home/wojtas/projects/brawler/tests/browser/journeys/j1-morning-review.spec.ts:145), which locates `.claim-row-highlighted` and asserts only visibility/text. That is still implementation-state-only and proves no paint. Add this spec to S2’s owned files and Playwright command, and perform the same canonical main-list-row before/after paint assertion required by ADR 0107.

   The two document specs are not literally attribute-only today: they also assert a non-transparent background ([activity.spec.ts:73](/home/wojtas/projects/brawler/tests/browser/activity.spec.ts:73), [spolka-documents.spec.ts:36](/home/wojtas/projects/brawler/tests/browser/spolka-documents.spec.ts:36)). However, that absolute check can pass if neutral rows later gain a background, and their `data-document-highlighted` assertions will be banned once PR #477 lands. Vitest attribute assertions under `src/**/*.test.tsx` are intentionally exempt and should remain.

8. **MEDIUM — the backup date contract must name the repository formatter.** The brief requires a localized date/time but does not specify how ([s2-backups-paint.md:5](/home/wojtas/projects/brawler/docs/plans/wave-2026-09b-contracts/s2-backups-paint.md:5)). Raw `createdAt` currently leaks into the UI ([BackupsSection.tsx:87](/home/wojtas/projects/brawler/src/screens/Diagnostics/BackupsSection.tsx:87), [BackupsSection.tsx:114](/home/wojtas/projects/brawler/src/screens/Diagnostics/BackupsSection.tsx:114)), while direct `toLocaleString` is banned. Require the shared datetime layer—most likely `formatListTimestamp(createdAt, locale, …)` for the human row title and last-backup figure—and test EN, PL, and null timestamps.

9. **MEDIUM — the documentation worklist points at sentences already changed and misses live contradictions.** Settings › Data storage is the right home: it is an existing normal-user tab ([SettingsScreen.tsx:58](/home/wojtas/projects/brawler/src/screens/Settings/SettingsScreen.tsx:58)), and adding an eleventh tab would worsen an already long subnav. But the base docs prematurely claim the move while code still hosts Backups in Diagnostics:

   - ADR 0032 decision 10 is already amended ([0032-search-and-backup-boundaries.md:58](/home/wojtas/projects/brawler/docs/adr/0032-search-and-backup-boundaries.md:58)).
   - Product spec and restore-flow step 1 already say Settings ([product-spec.md:314](/home/wojtas/projects/brawler/docs/product-spec.md:314), [ui-flows.md:397](/home/wojtas/projects/brawler/docs/ui-flows.md:397)).
   - Remaining contradictions say backups remain/recover from Diagnostics ([ui-information-architecture.md:379](/home/wojtas/projects/brawler/docs/ui-information-architecture.md:379), [ui-flows.md:407](/home/wojtas/projects/brawler/docs/ui-flows.md:407), [0032-search-and-backup-boundaries.md:82](/home/wojtas/projects/brawler/docs/adr/0032-search-and-backup-boundaries.md:82)).
   - The wiki’s Data storage inventory omits backups ([wiki/settings.md:24](/home/wojtas/projects/brawler/wiki/settings.md:24)).

   Replace the integrator instruction with those exact reconciliation targets. Diagnostics itself only directly depends on the import/render and backup CSS ([DiagnosticsScreen.tsx:20](/home/wojtas/projects/brawler/src/screens/Diagnostics/DiagnosticsScreen.tsx:20), [DiagnosticsScreen.tsx:605](/home/wojtas/projects/brawler/src/screens/Diagnostics/DiagnosticsScreen.tsx:605)); its density and reconciliation tests have no backup dependency.

10. **MEDIUM — ADR 0104’s already-inserted amendment is malformed.** The `Open tool:` amendment was inserted in the middle of the original verb sentence ([0104-frontend-v2-design-language.md:33](/home/wojtas/projects/brawler/docs/adr/0104-frontend-v2-design-language.md:33)), producing “Eight verbs cover the app: … Amendment … (apply a preset…)”. It also still says “Eight verbs” although the dictionary now contains eighteen. Move the family rule into a proper dated amendment and repair the decision text; do not append the brief’s proposed sentence a second time. The colon family itself passes `paletteCopy.test`: the gate explicitly accepts either `"<verb> "` or `"<verb>:"` ([paletteCopy.test.ts:55](/home/wojtas/projects/brawler/src/app/paletteCopy.test.ts:55)).

11. **MEDIUM — S1’s prescribed label grep misses most affected consumers.** The command at [s1-search-palette-feed.md:3](/home/wojtas/projects/brawler/docs/plans/wave-2026-09b-contracts/s1-search-palette-feed.md:3) searches only fundamentals/claims/coverage/feed. It misses functional consumers such as [analyst-recommendations.spec.ts:25](/home/wojtas/projects/brawler/tests/browser/analyst-recommendations.spec.ts:25), [decision-journal-layout.spec.ts:22](/home/wojtas/projects/brawler/tests/browser/decision-journal-layout.spec.ts:22), [ownership-layout.spec.ts:24](/home/wojtas/projects/brawler/tests/browser/ownership-layout.spec.ts:24), [quality-panel-layout.spec.ts:23](/home/wojtas/projects/brawler/tests/browser/quality-panel-layout.spec.ts:23), and [density-quality-docs.spec.ts:54](/home/wojtas/projects/brawler/tests/browser/density-quality-docs.spec.ts:54). Search every label in `SPOLKA_TOOL_COMMANDS`, not a four-label subset. Update live comments such as [toolRegistry.tsx:207](/home/wojtas/projects/brawler/src/screens/Spolka/toolRegistry.tsx:207) as well.

12. **MEDIUM — #478’s integration obligations need to be explicit.** Changing the single `"Feed"` PL entry correctly fans out to the workshop button ([route.ts:65](/home/wojtas/projects/brawler/src/screens/Spolka/route.ts:65)), the core-card destination ([SpolkaScreen.tsx:437](/home/wojtas/projects/brawler/src/screens/Spolka/SpolkaScreen.tsx:437)), and the tool heading ([toolRegistry.tsx:55](/home/wojtas/projects/brawler/src/screens/Spolka/toolRegistry.tsx:55)); other feed phrases already translate as kanał and should not be globally renamed. The new palette translation must likewise say `Otwórz narzędzie: Kanał`. After PR #477 merges, integration must remove the stale `Feed` exemption from `identicalEntries.json`, as #478 requires, and test all three rendered PL consumers—not only the toolbar regex.

For epic closure, the ADR audit required by [kanban.md:37](/home/wojtas/projects/brawler/docs/kanban.md:37) should explicitly verify:

- ADR 0104: every palette producer has `{actionKey, verb}`, all three command families are distinguishable and keyboard-visible, no retired tool labels remain, and Kanał is consistently Polish.
- ADR 0107: all 15 tools remain reachable; dirty/escape/company-switch behavior survives; Documents and canonical main-list Claims targets retain heading focus, scroll into view, persistent `aria-current`, and visible paint.
- ADR 0108: no dockview/cockpit/named-view/Open-view surface returns; notebook search/transcript destinations still land in the company tool.
- ADR 0109: Activity still opens from topbar and palette, remains status rather than an attention/failure counter, and its typed document destination lands on the exact visibly marked document.
- All five child cards are delivered/closed, PR #477’s helper and locale-allowlist integration is complete, and every decision has a real live-path invocation—not unit-test-only evidence.

Codex session ID: 01a07fe4-67bc-7d11-a785-3d46d6c6125a
Resume in Codex: codex resume 01a07fe4-67bc-7d11-a785-3d46d6c6125a
