READ-ONLY, do not edit files. Round 2 re-verify of your DO-NOT-SHIP review (branch `fix/dogfooding-v0.79-v0.81`, PR #468). Re-read the CURRENT tree (`git rev-parse HEAD`; `git diff 7964f1d5...HEAD`). Since your R1 the owner also dogfooded the PR binary and changed the design of #6/#5 — verify that too.

Owner round 2 (2026-09-07): (1) "too much empty space; the collapse should be dynamic, the table can fill the space" → no capacity cap; columns stretch to fill; capacity from the NATURAL width of the widest period cell (`naturalCellWidth` in `useVisiblePeriods.ts`: `Range.getBoundingClientRect` over the cell content, header label + origin chip and every body value, plus cell padding; Pozycje sums the per-column maxima across value/qoq/yoy); the Trend column folds at the S container tier and `stickyWidth` is a function reading its rendered min-width. (2) "the header was pinned, after expanding it stopped" → collapsed tables are NOT scroll containers (`overflow-x: clip; overflow-y: visible`) so the thead pins to `.spolka-tool-body`'s scroll (`.spolka-tool-body .fundamentals-panel { overflow-y: visible }` — the panel was a never-scrolling scroll container); expanded tables (`data-expanded`) become a bounded 2-axis box `max-height: min(60vh, 80cqh)` with the header pinned inside.

Per-finding status for you to verify (FIXED / NOT-FIXED with evidence):
1. Closed combobox Enter/Home/End → `useComboboxListbox.ts` returns early unless `isOpen`; `ComboboxField` blurs only when Enter was consumed; test `useComboboxListbox.test.tsx` "inert while the list is closed". (No Shift+J/K integration test added — say whether the unit test suffices.)
2. Capacity → natural widths (above); specs derive expectations from `data-visible-periods` and assert the collapsed table fills but never overflows its box (`fundamentals-periods.spec.ts`).
3. Documents retarget hidden by filter/search → `CompanyReportDocumentsPanel.tsx` effect resets `kindFilter`/`query` when the target exists in `rows` but not in `filteredRows`; test "a retarget hidden by the search resets the filter".
4. Expander column → real `<th scope="col">` with a visually hidden `Earlier periods` / `Wcześniejsze okresy` in both tables; the 120 px floor kept (`td.facts-matrix-expander { height: 120px }`) — no one-row browser case added; say if required.
5. Cap → `min(60vh, 80cqh)`, and only in the expanded state; collapsed state has no nested scroller at all; `.fundamentals-panel` no longer a scroll container under the Spółka tool body.
6. `.fundamentals-periods-table .fundamentals-periods-focus` outranks the zebra.
7. `FeedDetailContent` renders the shared red-flag chip above the generic body; test asserts `Red flag`.
8. `activity.spec.ts` asserts the nearest scrolling ancestor of every destination is `.ui-modal-body` and `.activity-panel` has `overflow-y: visible` (no inserted-scroller self-test — say if required).
9. `CoreKpiTable.test.tsx` asserts the `ui-zebra` class.
10. Comments rewritten (`toolRegistry.tsx` dokumenty case, `FundamentalsPanel.tsx` section-order comment); `foldDiacritics` maps ł/Ł explicitly; test in `SpolkaScreen.test.tsx`.

New attack surface from the round-2 design: (a) `overflow-x: clip` + `overflow-y: visible` — confirm Chromium keeps `visible` (no coercion to auto) and that a mis-measured capacity would only clip, never scroll; (b) `naturalCellWidth` on cells whose content wraps or contains `display:block` children (origin chip inside the inline-flex `.facts-matrix-period` wrapper); (c) the ResizeObserver recompute loop — capacity change → more columns render → does anything re-trigger measurement in a way that oscillates?; (d) `cqh` support in WebView2/Chromium of the pinned renderer; (e) the S tier: Trend hidden — does `density-companies.spec`/visual catalog still assert the trend column anywhere?; (f) the live spec `tests/live/wave-2026-09.live.spec.ts` header math (`.at(-2)`) with the hidden trend th and the new expander th.

Evidence so far: vitest scoped suites green (1722), Playwright wave specs 72/72 on chromium-wide+compact (remaining 3 projects, visual rebaseline, check-local, CI and a third live drive are pending at the time of this prompt — check the PR for their state).

Deliver: per-finding FIXED/NOT-FIXED with file:line evidence, any NEW findings (blockers first) with concrete fixes, then the verdict SHIP / SHIP-WITH-FIXES / DO-NOT-SHIP.
