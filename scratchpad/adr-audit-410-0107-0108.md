## ADR 0107 — Company View Paradigm

| # | Decision (short) | Implementation | Live path | Guard | Verdict | Evidence |
|---|---|---|---|---|---|---|
| 1 | Spółka screen, 3 zones, no docking engine, mode nav Dziś/Inbox/Spółka | `src/screens/Spolka/SpolkaScreen.tsx`; grid `src/styles/screens/spolka.css:127` `.spolka-core`; nav `src/app/navigation.ts:66-73` | `navGroups` "modes" → `AppShell.tsx` renders nav → `SpolkaScreen` mounted via `useSpolkaScreenWiring.tsx` from `AppStateRoot.tsx` | `SpolkaScreen.test.tsx` (36K), `paneLandmarks.test.tsx` | conforms | Modes = Today/Inbox/Spolka only (navigation.ts:66-73); `.spolka-core` is CSS grid, collapses to `.spolka-core-strip` (spolka.css:352) when a tool opens |
| 2 | Routes = discriminated union `{kind:"company",...}\|{kind:"namedView",...}`, `tool` closed 15-variant union | `src/screens/Spolka/route.ts:15-30` (`Tool`, exactly 15 `TOOL_KINDS`); live route state is `Section` enum + `selectedCompanyId` + `Tool` (`useSpolkaScreenWiring.tsx:25` `SpolkaTransition`), not a literal `{kind:"company"\|"namedView"}` object | `useSpolkaNavigate.ts` → `AppStateRoot.tsx` `navigate()` | `SpolkaScreen.test.tsx`, `route.ts` TS exhaustiveness (`satisfies readonly Tool["t"][]`) | partial | `Tool` union faithfully closed (15 variants confirmed by count); top-level route is `Section`(flat enum)+`companyId`+`tool`, not the literal ADR `{kind}` union — `namedView` arm never exists in live code (correctly, since ADR 0108 removed it), so intent (unrepresentable invalid combos) holds but the named shape doesn't |
| 3 | One composed `get_company_view` read model, one invocation, closed per-section error map | `src-tauri/src/commands/company_view.rs:795` `get_company_view`, registered `src-tauri/src/lib.rs:431`; `src/api/companyView.ts:24`; type `src/api/generated/CompanyView.ts:14` (`sectionErrors: CompanyViewSectionErrors`) | `SpolkaScreen.tsx:129` `useCommandQuery([companyId,...], () => getCompanyView(companyId))` — one call per company | `company_view_tests.rs` (`get_company_view_is_checkout_bounded`, etc.) | conforms | Single invocation per company confirmed at SpolkaScreen.tsx:129; `sectionErrors` closed map on `CompanyView` type |
| 4 | Daily OHLC candlesticks, log axis, ~3M window, shared `CandlestickChart`, `compute_price_context` | `src/ui/CandlestickChart.tsx:37`; used `SpolkaScreen.tsx:477` `scale="log"`; caption "3 months · daily candles (OHLC) · log scale" (SpolkaScreen.tsx:485); `compute_price_context` feeds `company_view.rs:232,601-602` | `get_company_view` → `price.candles` → `CandlestickChart` in `SpolkaScreen.tsx` | `CandlestickChart` primitive tests, `PrimitiveGallery.tsx` gallery entry | conforms | `scale="log"` wired at both `SpolkaScreen.tsx:485` and `PriceContextSection.tsx:234`; log-scale fallback-to-linear guard at `CandlestickChart.tsx:64` |
| 5 | Freeform frozen → (per 0108) fully REMOVED, not read-only | Cockpit/dockview/`cockpit_layouts`/"Dawny dashboard" absent from `src/` (see 0108 table) | n/a (removed) | migration guard tests below | conforms | grep for `CockpitScreen`, `dockview` (non-target), "Dawny dashboard", `dashboard:` in `src/**/*.ts(x)` (excl. tests) returns nothing; live state is full removal, matching 0108 not the original freeze text |
| 6 | Workshop dirty seam (`ToolHost{isDirty,discard}`) on every unmount incl. app close; company switch closes tool | `src/screens/Spolka/ToolHost.tsx:24-193`; app-close wiring `src/app/handleCloseRequested.ts:6-11` + `src/app/useAppLifecycleEffects.ts:166-167` (`isDirty: () => spolkaToolRef.current.isDirty()`, `ask: () => ...guardNavigation(...win.destroy())`); company-switch guard `useSpolkaNavigate.ts` (`guardNavigation` wraps every commit) | `getCurrentWindow().onCloseRequested` (Tauri) → `handleCloseRequested` → `spolkaTool.isDirty/guardNavigation`; `useSpolkaKeyboard.ts` Shift+J/K → `navigate(...)` with no `tool` key (closes tool, never retargets) | `handleCloseRequested.test.ts`, `SpolkaToolHost.test.tsx` ("switching company closes a clean tool", "discard calls discard() and proceeds", multi-handle registration tests) | conforms | App-close path genuinely calls into the same `isDirty`/`guardNavigation` seam as company-switch, not a separate ad hoc check |
| 6a | Amdt F3c: workshop bar APG toolbar, Ctrl+., H/L cycle, Escape→Overview | `role="toolbar"` `SpolkaScreen.tsx:596` (comment at :571 "APG toolbar... roving tabindex"); shortcut `app.focusWorkshop` `src/app/shortcuts.ts:18,117`; `useSpolkaKeyboard.ts` `cycleTool` | Shortcut registry → `useSpolkaKeyboard` → `navigate` | `SpolkaScreen.test.tsx`, `shortcuts.test.ts` | conforms | Toolbar role + roving tabindex comment, `cycleTool` wraps 0..WORKSHOP_TOOLS.length |
| 6b | Amdt dogfooding: 3-col header, `ComboboxField` picker, deep-link target contract (`aria-current`) | `ComboboxField` at `SpolkaScreen.tsx:238`; deep-link scroll/`aria-current` logic referenced in `SpolkaScreen.tsx` around target handling (per ADR text) | Header render in `SpolkaScreen.tsx` | `SpolkaScreen.test.tsx`, `toolPrimaries.test.tsx` | conforms | `ComboboxField` shared with ⌘K palette confirmed imported at SpolkaScreen.tsx:14 |

**Not conforming (ADR 0107):**
- #2 route union: the literal `{kind:"company"|"namedView"}` discriminated-union route type described in the ADR text does not exist in live code; the app instead composes `Section` + `companyId` + `Tool` (a `SpolkaTransition`). Functionally equivalent (invalid tool/company combos remain unrepresentable via the closed `Tool` union) but not the named shape the ADR specifies — worth a doc correction/pointer update at closure.

## ADR 0108 — Retire the Docking Engine

| # | Decision (short) | Implementation | Live path | Guard | Verdict | Evidence |
|---|---|---|---|---|---|---|
| 1 | No layout engine needed — CSS grid, workshop bar+ToolHost, dirty seam, container queries, ADR 0104 primitives serve every requirement | `.spolka-core` grid (`spolka.css:127`); `@container pane (max-width: 419px)` (`spolka.css:449`), narrow-window collapse (`spolka.css:156`); `ToolHost.tsx` | Same live path as ADR 0107 dec. 1/6 | Same tests as above + Playwright viewport matrix (`playwright.config.ts`, per CLAUDE.md) | conforms | Grid + container queries present and used by the real Spółka screen, not a demo |
| 2 | Field verdict — dockview/alternatives rejected; "less framework" recorded as deferred hypothesis, nothing built | `package.json` has no `dockview`, `react-resizable-panels`, `react-mosaic`, `flexlayout-react`, `golden-layout`, `rc-dock` entries | n/a — negative check | none by design (documented decision, not code) | conforms | grep across `package.json` for all six candidate libs returns zero matches |
| 3 | REMOVE dockview/cockpit screen/"Views" sidebar/"Open view:"/`Section` variant/4 IPC commands+store; `cockpit_layouts` dropped by migration 0152; shared panels → `src/screens/Spolka/panels/` | Migration `src-tauri/migrations/0152_drop_cockpit_layouts.sql` (`DROP TABLE IF EXISTS cockpit_layouts`); panels dir `src/screens/Spolka/panels/` (11 files: `RedFlagsSection.tsx`, `ShortPositionsSection.tsx`, `DecisionJournalSection.tsx`, `CompanyFeedPanel.tsx`, etc.) | Migration runner applies 0152 on every `open_in_memory_database()` / real DB upgrade; `src-tauri/src/lib.rs` invoke_handler has no `list/save/rename/delete_cockpit_layout` entries | `migration_safety.rs:6088` `migration_0152_drops_cockpit_layouts_on_a_fresh_database`, `migration_0152_drops_cockpit_layouts_carrying_a_legacy_row`; `scripts/check/retired-surface.mjs` (Makefile:216) scans **docs only** for `dockview`, `cockpit_layouts`, the 4 commands, "Research cockpit", `.cockpit-pane`, `cockpit-palette`, `Open view:` | conforms | Migration + two dedicated tests exist and pass the intended checks; grep of `src/**` (excl. `target/`, tests, migration history files) for `dockview`, `CockpitScreen`, `cockpit_layouts`, `list/save/rename/delete_cockpit_layout`, `"Open view:"`, `"Widoki"` group returns nothing live; `navigation.ts` sidebar groups are only modes/library/utilities |
| 3a | Amdt F4c S2: global Journal/Notebooks route retired; deep links → `navigateToCompanyNotebook`/`{t:"notatnik"}` | `src/screens/Spolka/route.ts:19-25` (`notatnik` tool + `NotebookToolIntent`); `navigateToCompanyNotebook` defined `AppStateRoot.tsx:804`, called by `useNotebookController.ts:78`, `useTranscriptController.ts:407`, `AppStateRoot.tsx:1083,1138` (feed-item, evidence/search deep links) | Every named deep-link caller (Inbox feed item, transcript "create note", evidence match, global search) routes through the single `navigateToCompanyNotebook` closure | `docs/retired-surface.json` entries (`NotebooksScreen`, `DecisionJournalGlobalPanel`, `app.openNotebooks`, "Journal (all companies)", etc.) enforced by `retired-surface.mjs` (docs-only); no `src/**` component named `NotebooksScreen`/`DecisionJournalGlobalPanel` found | conforms | grep of `src/**/*.ts(x)` for `NotebooksScreen`, `DecisionJournalGlobalPanel`, `screen.open.journal`, `app.openNotebooks` returns nothing; all 4+ deep-link call sites funnel into the one `navigateToCompanyNotebook` |
| 4 | Re-entry rule: multi-pane returns only via a new ADR triggered by a real journey | n/a | n/a | none — this is a process/governance rule, not a code artifact | not built (expected) | Correctly a documented policy only; no code gate should exist for "a future ADR must be written" — nothing to check beyond confirming no engine was silently reintroduced (see dec. 2/3) |

**Not conforming (ADR 0108):**
- None of the code-bearing decisions (1, 2, 3, 3a) show a gap. Note on guard strength: `retired-surface.mjs` (the only named automated guard) scans **only `docs/**.md` and `wiki/**.md`**, not `src/`— reintroduction of `dockview`/cockpit code in `src/` is caught only by (a) the package.json dependency being absent (import would fail to resolve) and (b) code-absence/review, not by a dedicated lint/knip rule scanning `src/`. Worth flagging at closure if a stronger src-level guard is wanted, though risk is low since the npm package itself is gone.
- Decision 4 is correctly "not built" — it's a governance rule; forcing a code artifact for it would be wrong.

**Adversarial checks performed:** migration 0152 exists and drops `cockpit_layouts` (confirmed, plus two dedicated regression tests); `docs/retired-surface.json` lists all the named tokens and is wired into `make check` via `scripts/check/retired-surface.mjs`; `ToolHost` is wired to the real Tauri `onCloseRequested` event (not just tool-switch) via `useAppLifecycleEffects.ts`; the Spółka screen is reached through the real `navGroups`/`AppShell`/`AppStateRoot` nav, not a standalone harness; Journal/Notebooks global route is genuinely absent from `src/` and all located deep-link call sites resolve through `navigateToCompanyNotebook`.