## Audit: ADR 0109 (Activity Center — Occurrence Ledger)

| # | Decision (short) | Implementation | Live path | Guard | Verdict | Evidence |
|---|---|---|---|---|---|---|
| 1 | Identity=domain task, typed resolver, gate over `registered_kinds()`, `corrupted` fallback | `jobs/activity_identity.rs` (`ActivityIdentity`, `corrupted()` at L94, per-kind resolvers L275+) | `commands/activity.rs::list_activity`/`compute_activity` (registered command, `lib.rs:434-435`) calls identity resolution per `job_runs` row | `jobs/activity_identity/tests.rs:195,273,293` — enumerates real `registered_kinds()` and asserts every kind resolves | conforms | Gate diffs the resolver match arm against the live registry; unregistered kinds excluded, malformed payload → `corrupted:<job_id>`. |
| 2 | `job_runs` occurrence table, exactly two write seams, atomic settle, retention | Migration `src-tauri/migrations/0153_job_runs.sql` (schema matches ADR verbatim); `storage/job_runs.rs::begin_attempt`(L81/L204)/`settle`(L107/L213, one `BEGIN IMMEDIATE`) | `jobs/queue.rs::dispatch` (L457-509) calls `begin_attempt` L590 after source lock, handler under `catch_unwind` L619 → `process_one`(L660)→`process_one_for_kinds`(L675)→`spawn_pools`(L726, `jobs/queue.rs`)→called from `lib.rs::run` L214 (real Tauri startup) | `jobs/queue_dispatch_tests.rs` (`begin_attempt_failure_skips_run_and_defers` L179, panic-containment tests L417+); `storage/job_runs.rs` inline tests L242,L266 | conforms | Dispatch seam verified end-to-end from app startup to the DB write; schema, statuses, atomic settle+prune all match. |
| 3 | Direct-activity RAII registry (`ActivityGuard`, Drop=interrupted), 5 instrumented cores, queue calls unwrapped cores | `storage/activity_registry.rs` — `ActivityGuard` (L124), `Drop::drop` settles `Interrupted` (L179-183), `start()` L191 | `start()` called live (non-test) from: `source_refresh.rs::sweep_adapters`(L385, per-adapter refresh in sweep), `refresh_source_direct`(L723)/`refresh_company_directories_direct`(L743) ← `commands/sources.rs::refresh_source`(L155, real Tauri command), `aggregator_fundamentals_pull.rs::run_aggregator_fundamentals_pull_direct`(L219), `backfill.rs::backfill_company_history_direct`(L96), `transcript_runner.rs::run_video_transcript_job`(L6) | `activity_registry.rs` inline tests: `direct_activity_panic_settles_interrupted`(L350), `a_begin_attempt_failure_still_yields_a_live_unrecorded_entry`(L440) | conforms | All 5 ADR-named cores instrumented; `refresh_source` Tauri command is a real reachable entry point. |
| 4 | Honest liveness states + startup reconciliation ordering | `jobs/activity_reconcile.rs::reconcile_on_startup`(L25): open occurrences→interrupted(L58), transcripts→failed(interrupted)(L74), autopilot(report-reading) orphans→failed(L97), sweep/reextraction-batch orphans→failed(L140); KPI runs never touched (comment L45-48) | `lib.rs::run`: KPI-run reclaim L173 → generic queue reclaim + KPI queue reconcile L186 → `activity_reconcile::reconcile_on_startup` L210 → `spawn_pools` (worker lanes) L215 — ordering pinned exactly as specified | `jobs/activity_reconcile/tests.rs` (submodule dir exists) | conforms | Startup order in `lib.rs` matches ADR literally; comment at L206-209 self-documents the pin. |
| 5 | Status ledger not notification; topbar never shows failure count | `commands/activity.rs::ActivitySummary`(L88-92) has only `active`,`queued`,`last_finished_at` — no failure field, structurally can't render one; `jobs/failure_surface.rs::FailureSurface` enum (L32-39) has exactly 3 variants (TodayAttention, SourcesAdapterHealth, AutopilotRunCard) — no Activity variant | `ActivityIndicator.tsx` renders only `active`/`queued`/`lastFinishedAt` (no danger tone; comment L17 explicitly bans danger tone/failure count) | `jobs/failure_surface/tests.rs` (enumeration-gate); no dedicated frontend test asserts "topbar never shows failure count" | partial | No direct regression test for the frontend claim — only type-shape + enum absence prevent it; a future field addition to `ActivitySummary` wouldn't redden a UI test. |
| 6 | Topbar Modal placement, shortcut suppression, palette entry, read-model polling | `AppShell.tsx:562` renders `<ActivityIndicator>` immediately after the Sources pill (L551-561); `ActivityPanel.tsx` uses `Modal` (L295-357); palette entry `activityCommand` L195-201 (`text("Open activity")`→"Otwórz aktywność" in `plText.ts:2265`); `useActivityController.ts` (15s summary / 5s panel poll, coalesced-resource posture L38+) | `AppShell.tsx` is the App root component; `shared/shortcuts/index.ts:102` checks `[aria-modal="true"]` to suppress shortcuts | `ActivityPanel.contract.test.tsx`; `useActivityController.test.tsx` | conforms | Placement, Modal primitive, palette copy, and polling cadence all verified in source. |

**Not conforming**

- Decision 5 (partial): `ActivitySummary` DTO and `FailureSurface` enum structurally prevent a failure count on the topbar, but no dedicated test (e.g. `ActivityIndicator.test.tsx`, which does not exist) asserts this — a future schema/UI change could silently reintroduce it undetected until manual QA.