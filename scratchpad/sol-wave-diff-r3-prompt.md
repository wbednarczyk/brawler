READ-ONLY, do not edit files. Round 3 re-verify (branch `fix/dogfooding-v0.79-v0.81`, PR #468). Re-read the CURRENT tree (`git rev-parse HEAD`, `git diff 7964f1d5...HEAD`); master is merged in now (conflict in docs/engineering-workflow.md resolved).

Your R2 blockers, as addressed — verify FIXED / NOT-FIXED with file:line evidence:
1. Capacity from the rendered slice → `useVisiblePeriods.ts`: a **measuring pass** — when `measureKey` (locale + period ids + row ids [+ granularity]) changes, `measuring` is true, the host renders EVERY period with `visibility: hidden` (`data-measuring` on the wrapper), a `useLayoutEffect` reads the widest natural period group before paint (`naturalCellWidth`: text ink via Range + padding + borders of the cell and of the stretched inner control), caches it in a ref, then capacity = `total*w ≤ clientWidth − fixed ? total : max(1, floor((clientWidth − fixed − expander)/w))` recomputed from the cache on every resize; measurer callbacks are kept in refs (`measurers.current`) so their identity never re-triggers effects; `measureFixedWidth`/`measureExpanderWidth` read rendered widths (corner `offsetWidth`, trend natural vs min-width when displayed, `--period-expander-width` CSS var). Pozycje sums per-column maxima over `[data-period-col]` skipping `display:none` columns. Fixture: `goodwillLegacy` in `overlays.ts` (oldest period widest: "1 234 mld PLN" vs "64 mld PLN"). Spec: `fundamentals-periods.spec.ts` `measure()` computes an INDEPENDENT expected capacity (expand → measure every column with the same definition → apply the contract → collapse → compare header count) and asserts the collapsed table fills but never overflows.
2. S-tier quarterly Pozycje clipped → `companies.css` `@container pane (max-width: 419px)`: `.fundamentals-periods-scroll { --period-kpi-width: 110px }` and `[data-period-col="qoq"], [data-period-col="yoy"] { display: none }`; `density-companies.spec.ts` now asserts `overflow-x: clip`, `scrollWidth ≤ clientWidth`, Δ columns hidden, value column visible at S; plus "Trend folds at S and shows at M".
3. `cqh` fallback → `max-height: 60vh` in the base rule, `min(60vh, 80cqh)` inside `@supports (max-height: 1cqh)`.
4. Docs drift → IA § Fundamentals rewritten (no periods list; origin chip in the newest header; warn chip in the section header), `wiki/company-view.md` ticket sentence, `fundamentals-sticky.spec.ts` header comment, dead `.fundamentals-autopilot*` CSS removed (`.periods-list`/`.period-item` are LIVE — `CustomKpiManager.tsx` uses them; comment fixed), static ticket cursor removed, PR body filler sentence replaced.
5. Trend S/M assertion added; live spec selects the newest period header via `thead th[data-period-cell]`.
R1 minor: `useComboboxListbox.test` now dispatches Home and asserts `aria-expanded` false.

Attack the measuring pass specifically: (a) does `measureKey` cover every input that changes cell widths (locale, tab rows, find-query filtering, period set, granularity, comparison rows)? (b) the hidden render: any flash or focus/scroll side effects (the expander column is not rendered while measuring; `data-visible-periods` is unset)? (c) `useLayoutEffect` ordering vs the ResizeObserver effect on first mount; (d) a period set of 0 (`total === 0`) and a remount; (e) Range measurement on `display:none` cells at S; (f) the collapsed `overflow-x: clip` when the single minimum group still does not fit (which tier/fixture could still produce that?); (g) `td.facts-matrix-expander { height: 120px }` with one body row.

Evidence at the time of writing: vitest 1722 green; Playwright wave specs 50/50 (wide, compact); remaining 3 projects, `make visual-update ALL=1`, `make check-visual`, `make check-local`, CI on the merged head and a third live drive are running/pending — check the PR for their state.

Deliver: per-finding FIXED/NOT-FIXED with evidence, NEW findings (blockers first) with concrete fixes, then the verdict SHIP / SHIP-WITH-FIXES / DO-NOT-SHIP.
