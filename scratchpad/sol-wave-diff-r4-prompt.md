READ-ONLY, do not edit files. Round 4 re-verify (branch `fix/dogfooding-v0.79-v0.81`, PR #468). Re-read the CURRENT tree (`git rev-parse HEAD`, `git diff 7964f1d5...HEAD`).

Your R3 blockers, as addressed — verify FIXED / NOT-FIXED with file:line evidence:
1. Facts capacity cached before provenance / surviving value edits → `src/screens/Companies/periodMeasureKeys.ts`: `factsMeasureKey` = locale + period ids/years/types + latest period id + origin tier + mixed flag + per row (definition id, valueKind, unit, metricKey) × per period (valueNumeric, asReportedValue, asReportedScale, currency, dataQuality, annotation marker); `periodsMeasureKey` = locale + granularity + axis keys + per series (metricKey, valueKind) × per cell (value, currency, deltaQoQ, deltaYoY, flags). `FundamentalsPanel.tsx` computes origin/latest BEFORE the hook and passes `factsMeasureKey(...)`; `FundamentalsPeriodsSection.tsx` passes `periodsMeasureKey({ locale, granularity, comparison })`. Unit tests `periodMeasureKeys.test.ts` (value edit, annotation, origin, mixed, locale, delta/flag/granularity all change the key).
2. Pozycje S-only width reused at M → `useVisiblePeriods` gained `measureVariant?: () => string`; the variant is captured during the measuring pass and compared on EVERY recompute (resize); a mismatch sets `measuredKey` to null → a new measuring pass with the displayed column set. Pozycje's variant = the displayed subset of `value,qoq,yoy` (computed `display` of the first cell of each). Hook unit test "re-measures when the presentation key or the displayed column set changes"; browser oracle `measurePositions()` in `fundamentals-periods.spec.ts` across S→M→L and L→M→S (expand → measure displayed columns per group → apply the contract → collapse → compare group count; collapsed table never overflows at each tier).
3. Comments: `shortcuts.ts` and `useSpolkaScreenWiring.tsx` no longer say `<select>`; density test title/comment describe the clipped-collapsed / scrolling-expanded contract; the Fundamentals hook comment describes the separately reserved expander.

Attack: (a) any remaining width input outside the keys (e.g. `localizedKpiLabel` changes are the sticky column, not a period cell — confirm; the header period label format; `tierLabel` text for the origin chip depends on `text()` — locale is in the key); (b) the variant check runs inside `recompute` which the layout effect also calls right after measuring — confirm no infinite re-measure when the variant reads `""` before cells exist (total 0 / first mount); (c) the ResizeObserver firing during the measuring render (hidden pass) — order of `setMeasuredKey(null)` vs the pass in flight; (d) `data-total-periods` used by the oracle is written by the host — confirm the oracle does not otherwise read the implementation's result; (e) anything in the merged master (#474/#475 docs) that the branch's docs now contradict.

Evidence at the time of writing: vitest 1726 green; Playwright on all 5 projects, `make visual-update ALL=1` (expected: no pixel change), `make check-visual`, `make check-local`, CI on the new head and a fourth live drive are running/pending — check the PR. Live drive 3 on `6eb97a67` (real DB) passed 1/1.

Deliver: per-finding FIXED/NOT-FIXED with evidence, NEW findings (blockers first) with concrete fixes, then the verdict SHIP / SHIP-WITH-FIXES / DO-NOT-SHIP.
