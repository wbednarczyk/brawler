Verdict: **DO-NOT-SHIP**. The fixes close most R1 findings, but queue settlement can still strand live rows, panic recovery bypasses terminal hooks, and parent outcomes remain incorrect.

I kept the review read-only and did not rerun `cargo nextest`; therefore the claimed “2302/2302 green” is not independently verified. The current worktree also contains an unrelated untracked `scratchpad/`, which is outside `master...HEAD`.

## R1 findings: current disposition

1. **PARTIAL — panics outside the handler catch.** An outer unwind boundary and `ClaimGuard` now exist at [queue.rs:126](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:126) and [queue.rs:253](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:253). However, the guard is disarmed before deferral is known to have succeeded at lines 298–301, 332–335 and 362–365, and ordinary settlement errors are abandoned after disarming at lines 394–402. See new finding 1.

2. **FIXED — missing queue row left the occurrence running.** Both settlement methods close the exact occurrence and then return `JobQueueRowMissingDuringSettle`: [jobs.rs:364](/home/wojtas/projects/brawler/src-tauri/src/storage/jobs.rs:364), [jobs.rs:401](/home/wojtas/projects/brawler/src-tauri/src/storage/jobs.rs:401). The storage tests cover failed and successful outcomes at [job_queue.rs:304](/home/wojtas/projects/brawler/src-tauri/src/storage/tests/job_queue.rs:304) and [job_queue.rs:353](/home/wojtas/projects/brawler/src-tauri/src/storage/tests/job_queue.rs:353). Dispatch does not reach terminal hooks when that error propagates.

3. **FIXED — direct registry could hide or overwrite a live occurrence.** Entries are keyed by occurrence/synthetic handles rather than `activity_key` at [activity_registry.rs:92](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_registry.rs:92); failed settlement stays registered as stalled at lines 157–177; failed `begin_attempt` creates `Unrecorded` at lines 229–248; settle plus pruning is one IMMEDIATE transaction at [job_runs.rs:213](/home/wojtas/projects/brawler/src-tauri/src/storage/job_runs.rs:213). The same-key and settle-failure tests are at [activity_registry.rs:368](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_registry.rs:368) and [activity_registry.rs:403](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_registry.rs:403).

4. **PARTIAL — `recent` ignored domain status and made `partial` unreachable.** Report-reading and KPI domain overrides now exist at [activity.rs:185](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:185), with tests at [activity_read_model_tests.rs:297](/home/wojtas/projects/brawler/src-tauri/src/commands/activity_read_model_tests.rs:297). Mixed sweep/batch outcomes are still wrong because the override evaluates progress only when the parent status is `failed`; production completes sweeps even when `runs_failed > 0`. See new finding 3.

5. **FIXED — one task appeared across multiple sections and stalled work drove the spinner.** The read model now builds one precedence-ordered keyed set at [activity.rs:606](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:606), excludes its keys from `recent`, and counts only `running` keys in the summary at [activity.rs:774](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:774). The cross-section sweep test is at [activity_read_model_tests.rs:359](/home/wojtas/projects/brawler/src-tauri/src/commands/activity_read_model_tests.rs:359).

6. **FIXED — transcript post-provider errors left the domain row running invisibly.** All provider/segment/completion branches now funnel through the finalizer at [transcript_runner.rs:45](/home/wojtas/projects/brawler/src-tauri/src/jobs/transcript_runner.rs:45) and [transcript_runner.rs:111](/home/wojtas/projects/brawler/src-tauri/src/jobs/transcript_runner.rs:111). If even the domain failure write fails, the occurrence closes and the domain row derives as stalled, covered at [transcript_runner.rs:247](/home/wojtas/projects/brawler/src-tauri/src/jobs/transcript_runner.rs:247).

7. **FIXED — failed queued backfill was recorded as success.** `run_company_backfill_job` now converts failed `BackfillProgress` into `Err`, entering normal queue failure/retry settlement: [backfill.rs:349](/home/wojtas/projects/brawler/src-tauri/src/jobs/backfill.rs:349).

8. **PARTIAL — direct refresh bypasses and no awaited-path gate.** The lookup path now uses the direct wrapper at [companies.rs:62](/home/wojtas/projects/brawler/src-tauri/src/commands/companies.rs:62), and `DIRECT_PATHS` plus a source-scan gate exist at [activity_awaited_paths.rs:44](/home/wojtas/projects/brawler/src-tauri/src/jobs/activity_awaited_paths.rs:44). Current Tauri and MCP aggregator/backfill paths call the same `_direct` wrappers. The `rebuild_fundamentals` exception matches ADR 0109’s explicit headless-rebuild exclusion, so I accept it. But every `_direct` wrapper can still silently skip registration when its preliminary identity checkout fails; see new finding 4.

9. **FIXED — KPI identity trusted the payload rather than the claimed ID.** Validate and commit identities use `parse_job_id(job_id)` at [activity_identity.rs:412](/home/wojtas/projects/brawler/src-tauri/src/jobs/activity_identity.rs:412); mismatched payload coverage is at [activity_identity/tests.rs:317](/home/wojtas/projects/brawler/src-tauri/src/jobs/activity_identity/tests.rs:317). Handler preflight independently checks ID/payload coherence.

10. **FIXED — ISO-string seven-day boundary.** Both sides now use the same millisecond RFC3339 shape at [activity_reads.rs:244](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_reads.rs:244). The exact-cutoff regression test begins at [activity_reads.rs:693](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_reads.rs:693).

11. **FIXED — stale controller responses and timers.** Closing/disabling advances a generation and clears pending work at [useActivityController.ts:67](/home/wojtas/projects/brawler/src/app/useActivityController.ts:67); responses validate it at lines 84–93; view polling is gated by `enabled && open` at line 133. Close/reopen and disable races are covered at [useActivityController.test.tsx:148](/home/wojtas/projects/brawler/src/app/useActivityController.test.tsx:148) and [useActivityController.test.tsx:224](/home/wojtas/projects/brawler/src/app/useActivityController.test.tsx:224).

12. **PARTIAL — reconciliation atomicity, wildcard matching, and fail-closed startup.** Bulk occurrence interruption is one IMMEDIATE transaction at [activity_reconcile.rs:58](/home/wojtas/projects/brawler/src-tauri/src/jobs/activity_reconcile.rs:58), and exact stage IDs are checked by [autopilot_liveness.rs:16](/home/wojtas/projects/brawler/src-tauri/src/jobs/autopilot_liveness.rs:16). KPI rows remain explicitly untouched at [activity_reconcile.rs:45](/home/wojtas/projects/brawler/src-tauri/src/jobs/activity_reconcile.rs:45). I accept the recorded design rebuttal that ledger reconciliation must not block core worker lanes; the implementation logs and proceeds at lines 18–44. Thus the original multi-part finding is only partially implemented, but the remaining difference is accepted design rather than a new blocker.

13. **PARTIAL — read-model errors were swallowed into plausible empty/default data.** Top-level queue/domain list reads now propagate errors, but nested reads still use `.ok()`, `unwrap_or`, and `Vec::new`: [activity.rs:124](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:124), [activity.rs:211](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:211), [activity_reads.rs:507](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_reads.rs:507). See new finding 5.

14. **FIXED — missing parent members, progress failures, backfill progress, and attempt count.** `members` is in the Rust and generated DTOs at [activity.rs:58](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:58) and [ActivityItem.ts:14](/home/wojtas/projects/brawler/src/api/generated/ActivityItem.ts:14); backfill progress comes from live `BackfillProgress` at [activity.rs:139](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:139); queued attempts use `job_queue.attempts` at [activity.rs:336](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:336).

15. **PARTIAL — UI language/primitive/copy violations.** I accept both recorded rebuttals:

   - `Otwórz …` destination actions match the owner-approved row convention and remain `kind="destination"` at [ActivityPanel.tsx:138](/home/wojtas/projects/brawler/src/shared/components/activity/ActivityPanel.tsx:138).
   - `Try again` matches the Alerts precedent and correctly remains `kind="control"` at [ActivityPanel.tsx:297](/home/wojtas/projects/brawler/src/shared/components/activity/ActivityPanel.tsx:297).

   `pluralNoun`, filename-only mono treatment, icon `Button`, and localized modal close are fixed. Status copy and one Polish backend subject are still defective; see new finding 6.

16. **FIXED — 100k read gate was vacuous.** The test now explains the exact production SQL, asserts the intended finished-time index and rejects a table scan/temp B-tree at [activity_reads.rs:741](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_reads.rs:741). The 500-row fetch is consistent with the ledger’s global 500-finished-row retention, after which the 40-key cap is applied.

17. **PARTIAL — missing or weak tests.** I accept both rebuttals:

   - Fresh dual-execution fidelity can deterministically seed only the queued transcript, and both runtimes execute the real `list_activity` path.
   - The shared direct wrappers are the unit-testable seams; the Tauri and MCP call sites do invoke them.

   The suite is substantially stronger, but several assertions remain incomplete or encode a known defect; see new finding 8.

18. **REBUTTED-ACCEPTED — `AppStateRoot.tsx` pin increase.** The plan now names the reviewed raise, the baseline is exactly 1865 at [file-size-baseline.json:37](/home/wojtas/projects/brawler/file-size-baseline.json:37), and the six added lines are composition-only wiring in [AppStateRoot.tsx:476](/home/wojtas/projects/brawler/src/app/AppStateRoot.tsx:476), [AppStateRoot.tsx:1028](/home/wojtas/projects/brawler/src/app/AppStateRoot.tsx:1028), and [AppStateRoot.tsx:1519](/home/wojtas/projects/brawler/src/app/AppStateRoot.tsx:1519). I accept the raise.

## Current-tree findings

1. **BLOCKER — `ClaimGuard` is disarmed before the claimed row has safely left `running`.** All three defer branches call `disarm()` before `store.defer()` at [queue.rs:298](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:298), [queue.rs:332](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:332), and [queue.rs:362](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:362). If defer errors or panics, the claimed queue row remains `running` and the fallback is disabled. Ordinary settlement similarly disarms even when settlement returned an error at [queue.rs:388](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:388). The new test explicitly asserts that both queue row and occurrence remain `running` after a settlement failure at [queue_tests.rs:775](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue_tests.rs:775)—exactly the live-app leak this feature was meant to prevent.

   Fix: disarm only after a successful defer/settlement or the special missing-row result known to have committed the occurrence. For ambiguous storage errors, leave the guard armed and perform an idempotent recovery settlement; if that also fails, retain an explicit queue-settle-failed handle that the read model renders `stalled`. Add injected defer-error and settle-error tests asserting no item remains `running`.

2. **BLOCKER — outer-panic terminal failures bypass both notification and domain terminalization hooks.** `ClaimGuard` is created outside the `catch_unwind` closure at [queue.rs:253](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:253), so its comment claiming Drop has already run inside the `Err` branch is false; it drops only when `dispatch` returns. That Drop may terminally fail the row at [queue.rs:174](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:174), but `surface_terminal_failure` and `on_terminal_failure` are called only by the ordinary error branch at [queue.rs:401](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue.rs:401). A last-attempt scaffolding panic can therefore terminalize a KPI queue row without failing its KPI domain run or raising its required failure surface.

   Fix: make panic recovery explicit in the `catch_unwind` error arm and return whether it retried or terminalized; invoke the same terminal hooks when terminal. Keep Drop solely as the final emergency fallback. Add a max-attempt scaffolding-panic test with a hook-counting handler.

3. **BLOCKER — completed parents with mixed member outcomes do not become `partial`.** The override checks progress only in the `"failed"` arm at [activity.rs:207](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:207). Yet history sweeps deliberately store `status='completed'` even when `runs_failed > 0` at [history_sweeps.rs:151](/home/wojtas/projects/brawler/src-tauri/src/storage/history_sweeps.rs:151). Once member jobs finish, the newest child occurrence can therefore make the whole parent appear either “Completed” or “Did not finish,” depending on which child happened to finish last, instead of deriving the parent’s mixed outcome.

   Fix: derive sweep/batch terminal status from all `enqueued_run_ids_json` members regardless of whether the parent row says `completed` or `failed`: mixed done/failed → `partial`, all failed → `failed`, all successful → `succeeded`. Add completed-parent tests with success-last and failure-last ordering.

4. **BLOCKER — awaited direct work still runs without any registry entry when identity checkout fails.** The wrappers turn checkout failure into `None`, then simply omit the guard: [source_refresh.rs:408](/home/wojtas/projects/brawler/src-tauri/src/jobs/source_refresh.rs:408), [source_refresh.rs:737](/home/wojtas/projects/brawler/src-tauri/src/jobs/source_refresh.rs:737), [source_refresh.rs:763](/home/wojtas/projects/brawler/src-tauri/src/jobs/source_refresh.rs:763), [aggregator_fundamentals_pull.rs:223](/home/wojtas/projects/brawler/src-tauri/src/jobs/aggregator_fundamentals_pull.rs:223), and [backfill.rs:98](/home/wojtas/projects/brawler/src-tauri/src/jobs/backfill.rs:98). This defeats the new `Unrecorded` registry variant: `start()` is never called, yet network work proceeds.

   Fix: provide identity constructors that tolerate unavailable lookup data by falling back to adapter/company IDs, and always call `activity_registry::start`. Then a failed `begin_attempt` correctly produces `Unrecorded`. Add a poisoned-checkout test proving the core runs with a visible stalled/unrecorded registry entry.

5. **MAJOR — nested read failures still silently alter status, progress, members, and grouping.** `parent_progress_for` converts SQL errors to absent progress at [activity.rs:124](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:124); parent override converts failures to `(0,0,0)` at [activity.rs:211](/home/wojtas/projects/brawler/src-tauri/src/commands/activity.rs:211); `member_subjects` suppresses query and JSON failures at [activity_reads.rs:507](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_reads.rs:507). This contradicts the controller’s “last-known-good plus typed error” contract.

   Fix: return `StorageResult<Option<_>>`/`StorageResult<Vec<_>>` from these helpers and propagate `?`. Reserve fallbacks only for genuinely missing optional domain rows, distinguished with `OptionalExtension`.

6. **MAJOR — Activity copy still lies and still leaks Polish into English.** The status map renders `succeeded` as “Completed” and `failed` as “Did not finish” at [activityLabels.ts:68](/home/wojtas/projects/brawler/src/shared/components/activity/activityLabels.ts:68); those are not the ledger’s exact outcomes and collapse “failed” with “interrupted.” The registry-refresh identity also stores the composed Polish subject `Rejestr spółek GPW/NewConnect` at [activity_identity.rs:240](/home/wojtas/projects/brawler/src-tauri/src/jobs/activity_identity.rs:240), which appears unchanged in English UI.

   Fix: use explicit success/failure labels matching the status vocabulary, with PL translations, and make registry refresh’s subject empty or a true raw proper name so the localized family label carries the prose.

7. **MAJOR — canonical docs drift from the shipped DTO and query.** `ActivityItem.members` exists in generated TypeScript at [ActivityItem.ts:14](/home/wojtas/projects/brawler/src/api/generated/ActivityItem.ts:14) but is absent from the normative JSON and field rules in [contracts.md:1578](/home/wojtas/projects/brawler/docs/contracts.md:1578). Data-model claims SQL performs `MAX(id) GROUP BY activity_key` at [data-model.md:1136](/home/wojtas/projects/brawler/docs/data-model.md:1136), while production performs a finished-time ordered range scan and Rust collapse at [activity_reads.rs:244](/home/wojtas/projects/brawler/src-tauri/src/storage/activity_reads.rs:244). The wiki claims queued rows show their next-attempt time at [activity.md:27](/home/wojtas/projects/brawler/wiki/activity.md:27), but the DTO carries no `availableAt` and the UI shows `startedAt`/creation time.

   Fix: document `members`, describe the actual ordered candidate scan plus Rust collapse, and either add a real next-attempt field or remove the wiki claim.

8. **MAJOR — some new tests are still vacuous, incomplete, or assert the defect.**

   - [queue_tests.rs:775](/home/wojtas/projects/brawler/src-tauri/src/jobs/queue_tests.rs:775) asserts that a finished handler’s occurrence remains `running`.
   - [activity_tests.rs:172](/home/wojtas/projects/brawler/src-tauri/src/commands/activity_tests.rs:172) tests 41 distinct rows, so it does not prove collapse-before-cap under duplicate-heavy input.
   - The planned five-stage autopilot-plus-retries case is replaced by only two source-refresh attempts at [activity_tests.rs:199](/home/wojtas/projects/brawler/src-tauri/src/commands/activity_tests.rs:199).
   - The live test exposes only `target.kind` and, for every company destination, merely asserts that some company view opened at [f3d-activity.live.spec.ts:90](/home/wojtas/projects/brawler/tests/live/f3d-activity.live.spec.ts:90). It cannot detect a Coverage/Overview/Documents mix-up or the wrong document ID. It also checks only the first five rows, not “every row.”
   - The exact planned “retention after startup reconciliation” and concrete manual-refresh panic→interrupted scenarios are absent; only generic registry Drop and settlement retention are tested.

   Fix: replace the settle-failure expectation with eventual stalled/terminal recovery; seed duplicate-heavy cap input; use five real stage identities; expose/assert target tool and document ID in the live harness; and add the two missing planned scenarios.

The CLI guard kept every repository operation non-mutating, and structural caller tracing was used to verify the Tauri/MCP/direct-core relationships rather than relying on the new source-scan test alone.

Codex session ID: 01a06b71-69c0-7293-868b-410699df3638
Resume in Codex: codex resume 01a06b71-69c0-7293-868b-410699df3638
